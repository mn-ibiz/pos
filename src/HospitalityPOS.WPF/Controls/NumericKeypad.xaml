<UserControl x:Class="HospitalityPOS.WPF.Controls.NumericKeypad"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="260"
             x:Name="Root">

    <UserControl.Resources>
        <!-- Keypad Button Style -->
        <Style x:Key="KeypadButton" TargetType="Button">
            <Setter Property="Height" Value="60"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#3D3D5C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#22C55E"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4E4E6E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Clear Button Style (Red) -->
        <Style x:Key="ClearButton" TargetType="Button" BasedOn="{StaticResource KeypadButton}">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B91C1C"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EF4444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Backspace Button Style (Orange) -->
        <Style x:Key="BackspaceButton" TargetType="Button" BasedOn="{StaticResource KeypadButton}">
            <Setter Property="Background" Value="#D97706"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B45309"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F59E0B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <UniformGrid Rows="4" Columns="3">
            <!-- Row 1: 7, 8, 9 -->
            <Button Content="7" Style="{StaticResource KeypadButton}" Click="DigitButton_Click" Tag="7"/>
            <Button Content="8" Style="{StaticResource KeypadButton}" Click="DigitButton_Click" Tag="8"/>
            <Button Content="9" Style="{StaticResource KeypadButton}" Click="DigitButton_Click" Tag="9"/>

            <!-- Row 2: 4, 5, 6 -->
            <Button Content="4" Style="{StaticResource KeypadButton}" Click="DigitButton_Click" Tag="4"/>
            <Button Content="5" Style="{StaticResource KeypadButton}" Click="DigitButton_Click" Tag="5"/>
            <Button Content="6" Style="{StaticResource KeypadButton}" Click="DigitButton_Click" Tag="6"/>

            <!-- Row 3: 1, 2, 3 -->
            <Button Content="1" Style="{StaticResource KeypadButton}" Click="DigitButton_Click" Tag="1"/>
            <Button Content="2" Style="{StaticResource KeypadButton}" Click="DigitButton_Click" Tag="2"/>
            <Button Content="3" Style="{StaticResource KeypadButton}" Click="DigitButton_Click" Tag="3"/>

            <!-- Row 4: C, 0, DEL -->
            <Button Content="C" Style="{StaticResource ClearButton}" Click="ClearButton_Click" ToolTip="Clear"/>
            <Button Content="0" Style="{StaticResource KeypadButton}" Click="DigitButton_Click" Tag="0"/>
            <Button Content="DEL" Style="{StaticResource BackspaceButton}" Click="BackspaceButton_Click" FontSize="16" ToolTip="Delete"/>
        </UniformGrid>
    </Grid>
</UserControl>
