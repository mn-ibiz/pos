<UserControl x:Class="HospitalityPOS.WPF.Views.HotelPMSIntegrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1400" Background="#1E1E2E">
    <UserControl.Resources><BooleanToVisibilityConverter x:Key="BoolToVisConverter" /></UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <Button Content="Back" Command="{Binding GoBackCommand}" Background="#3D3D5C" Foreground="White" BorderThickness="0" Padding="16,12" Cursor="Hand"/>
            <TextBlock Grid.Column="1" Text="Hotel PMS Integration" FontSize="28" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="Sync Now" Command="{Binding SyncNowCommand}" Background="#0284C7" Foreground="White" BorderThickness="0" Padding="24,12" Margin="0,0,8,0" Cursor="Hand"/>
                <Button Content="+ Configure PMS" Command="{Binding ConfigurePMSCommand}" Background="#4F46E5" Foreground="White" BorderThickness="0" Padding="24,12" Cursor="Hand"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Connection Status" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding ConnectionStatus}" FontSize="18" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Active Guest Folios" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding ActiveFolioCount}" FontSize="24" FontWeight="Bold" Foreground="#2196F3" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="2" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Pending Charges" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding PendingChargesTotal, StringFormat='KSh {0:N0}'}" FontSize="24" FontWeight="Bold" Foreground="#FF9800" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="3" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Last Sync" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding LastSyncTime, StringFormat='{}{0:HH:mm}'}" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/></StackPanel>
            </Border>
        </Grid>
        <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedTabIndex}" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem"><Setter Property="Background" Value="#2D2D44"/><Setter Property="Foreground" Value="#9CA3AF"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TabItem"><Border Name="Border" Background="#2D2D44" CornerRadius="8,8,0,0" Margin="0,0,4,0"><ContentPresenter ContentSource="Header" Margin="20,12"/></Border><ControlTemplate.Triggers><Trigger Property="IsSelected" Value="True"><Setter TargetName="Border" Property="Background" Value="#4F46E5"/><Setter Property="Foreground" Value="White"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
            </TabControl.Resources>
            <TabItem Header="Guest Folios">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding GuestFolios}" SelectedItem="{Binding SelectedFolio}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Room" Binding="{Binding RoomNumber}" Width="80"/>
                            <DataGridTextColumn Header="Guest Name" Binding="{Binding GuestName}" Width="*"/>
                            <DataGridTextColumn Header="Check-In" Binding="{Binding CheckInDate, StringFormat='{}{0:dd MMM}'}" Width="100"/>
                            <DataGridTextColumn Header="Check-Out" Binding="{Binding CheckOutDate, StringFormat='{}{0:dd MMM}'}" Width="100"/>
                            <DataGridTextColumn Header="POS Charges" Binding="{Binding POSCharges, StringFormat='KSh {0:N0}'}" Width="120"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                            <DataGridTemplateColumn Header="Actions" Width="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="View" Command="{Binding DataContext.ViewFolioCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#4F46E5" Foreground="White" Padding="8,4" Margin="0,0,4,0" BorderThickness="0" Cursor="Hand"/>
                                            <Button Content="Post" Command="{Binding DataContext.PostToFolioCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#059669" Foreground="White" Padding="8,4" BorderThickness="0" Cursor="Hand"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
            <TabItem Header="Pending Charges">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding PendingCharges}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding ChargeDate, StringFormat='{}{0:dd MMM HH:mm}'}" Width="140"/>
                            <DataGridTextColumn Header="Room" Binding="{Binding RoomNumber}" Width="80"/>
                            <DataGridTextColumn Header="Guest" Binding="{Binding GuestName}" Width="*"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat='KSh {0:N0}'}" Width="120"/>
                            <DataGridTemplateColumn Header="Action" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Post Now" Command="{Binding DataContext.PostChargeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#059669" Foreground="White" Padding="12,6" BorderThickness="0" Cursor="Hand"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
            <TabItem Header="Sync History">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding SyncHistory}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Timestamp" Binding="{Binding SyncTime, StringFormat='{}{0:dd MMM HH:mm:ss}'}" Width="160"/>
                            <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="100"/>
                            <DataGridTextColumn Header="Records" Binding="{Binding RecordCount}" Width="80"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                            <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="100"/>
                            <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
            <TabItem Header="Configuration">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="16">
                        <StackPanel MaxWidth="600">
                            <TextBlock Text="PMS Connection Settings" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,16"/>
                            <TextBlock Text="PMS Type" Foreground="#9CA3AF" Margin="0,0,0,4"/>
                            <ComboBox ItemsSource="{Binding PMSTypes}" SelectedItem="{Binding SelectedPMSType}" Background="#3D3D5C" Foreground="White" Margin="0,0,0,16"/>
                            <TextBlock Text="Server URL" Foreground="#9CA3AF" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding ServerUrl}" Background="#3D3D5C" Foreground="White" Padding="12,8" Margin="0,0,0,16" BorderThickness="0"/>
                            <TextBlock Text="API Key" Foreground="#9CA3AF" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding ApiKey}" Background="#3D3D5C" Foreground="White" Padding="12,8" Margin="0,0,0,16" BorderThickness="0"/>
                            <TextBlock Text="Sync Interval (minutes)" Foreground="#9CA3AF" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding SyncInterval}" Background="#3D3D5C" Foreground="White" Padding="12,8" Margin="0,0,0,24" BorderThickness="0"/>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Test Connection" Command="{Binding TestConnectionCommand}" Background="#0284C7" Foreground="White" Padding="24,12" Margin="0,0,8,0" BorderThickness="0" Cursor="Hand"/>
                                <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Background="#059669" Foreground="White" Padding="24,12" BorderThickness="0" Cursor="Hand"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </TabItem>
        </TabControl>
        <Border Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}" Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"><ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#4F46E5"/><TextBlock Text="Loading..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/></StackPanel>
        </Border>
    </Grid>
</UserControl>
