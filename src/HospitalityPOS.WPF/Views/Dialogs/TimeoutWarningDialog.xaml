<Window x:Class="HospitalityPOS.WPF.Views.Dialogs.TimeoutWarningDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Session Timeout Warning"
        Height="280"
        Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#2D2D44"
        AllowsTransparency="True"
        Topmost="True">

    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="MinHeight" Value="50"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#4E4E6E" BorderThickness="2" CornerRadius="12">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0"
                       Text="Session Timeout Warning"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="White"
                       HorizontalAlignment="Center"/>

            <!-- Message -->
            <TextBlock Grid.Row="1"
                       Text="You will be logged out in"
                       FontSize="16"
                       Foreground="#A0A0B0"
                       HorizontalAlignment="Center"
                       Margin="0,24,0,8"/>

            <!-- Countdown -->
            <StackPanel Grid.Row="2"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <Border Background="#1E1E2E"
                        CornerRadius="50"
                        Width="100"
                        Height="100"
                        Padding="8">
                    <TextBlock x:Name="CountdownText"
                               Text="{Binding SecondsRemaining}"
                               FontSize="48"
                               FontWeight="Bold"
                               Foreground="#FF6B6B"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="seconds"
                           FontSize="14"
                           Foreground="#8888AA"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,16,0,0">
                <Button x:Name="StayLoggedInButton"
                        Content="Stay Logged In"
                        Style="{StaticResource DialogButton}"
                        Background="#4CAF50"
                        Click="StayLoggedInButton_Click"/>
                <Button x:Name="LogoutNowButton"
                        Content="Logout Now"
                        Style="{StaticResource DialogButton}"
                        Background="#E74C3C"
                        Click="LogoutNowButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
