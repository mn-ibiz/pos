<UserControl x:Class="HospitalityPOS.WPF.Views.PermissionOverrideView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1400" Background="#1E1E2E">
    <UserControl.Resources><BooleanToVisibilityConverter x:Key="BoolToVisConverter" /></UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <Button Content="Back" Command="{Binding GoBackCommand}" Background="#3D3D5C" Foreground="White" BorderThickness="0" Padding="16,12" Cursor="Hand"/>
            <TextBlock Grid.Column="1" Text="Permission Override Management" FontSize="28" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <ComboBox ItemsSource="{Binding DateRangeOptions}" SelectedItem="{Binding SelectedDateRange}" Background="#3D3D5C" Foreground="White" Width="120" Margin="0,0,8,0"/>
                <Button Content="Export Audit" Command="{Binding ExportAuditCommand}" Background="#059669" Foreground="White" BorderThickness="0" Padding="24,12" Cursor="Hand"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Total Overrides" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding TotalOverrideCount}" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Pending Approval" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding PendingApprovalCount}" FontSize="24" FontWeight="Bold" Foreground="#FF9800" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="2" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Approved Today" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding ApprovedTodayCount}" FontSize="24" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="3" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Denied Today" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding DeniedTodayCount}" FontSize="24" FontWeight="Bold" Foreground="#F44336" HorizontalAlignment="Center"/></StackPanel>
            </Border>
        </Grid>
        <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedTabIndex}" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem"><Setter Property="Background" Value="#2D2D44"/><Setter Property="Foreground" Value="#9CA3AF"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TabItem"><Border Name="Border" Background="#2D2D44" CornerRadius="8,8,0,0" Margin="0,0,4,0"><ContentPresenter ContentSource="Header" Margin="20,12"/></Border><ControlTemplate.Triggers><Trigger Property="IsSelected" Value="True"><Setter TargetName="Border" Property="Background" Value="#4F46E5"/><Setter Property="Foreground" Value="White"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
            </TabControl.Resources>
            <TabItem Header="Pending Requests">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding PendingRequests}" SelectedItem="{Binding SelectedRequest}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Requested" Binding="{Binding RequestedAt, StringFormat='{}{0:dd MMM HH:mm}'}" Width="140"/>
                            <DataGridTextColumn Header="User" Binding="{Binding RequestedBy}" Width="*"/>
                            <DataGridTextColumn Header="Permission" Binding="{Binding PermissionName}" Width="180"/>
                            <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="200"/>
                            <DataGridTemplateColumn Header="Actions" Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Approve" Command="{Binding DataContext.ApproveRequestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#059669" Foreground="White" Padding="8,4" Margin="0,0,4,0" BorderThickness="0" Cursor="Hand"/>
                                            <Button Content="Deny" Command="{Binding DataContext.DenyRequestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#DC2626" Foreground="White" Padding="8,4" BorderThickness="0" Cursor="Hand"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
            <TabItem Header="Active Overrides">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding ActiveOverrides}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="*"/>
                            <DataGridTextColumn Header="Permission" Binding="{Binding PermissionName}" Width="180"/>
                            <DataGridTextColumn Header="Granted By" Binding="{Binding GrantedBy}" Width="120"/>
                            <DataGridTextColumn Header="Granted At" Binding="{Binding GrantedAt, StringFormat='{}{0:dd MMM HH:mm}'}" Width="140"/>
                            <DataGridTextColumn Header="Expires" Binding="{Binding ExpiresAt, StringFormat='{}{0:dd MMM HH:mm}'}" Width="140"/>
                            <DataGridTemplateColumn Header="Action" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Revoke" Command="{Binding DataContext.RevokeOverrideCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#DC2626" Foreground="White" Padding="12,6" BorderThickness="0" Cursor="Hand"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
            <TabItem Header="Audit Log">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <Grid Margin="16">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="Filter by Permission:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox ItemsSource="{Binding PermissionFilters}" SelectedItem="{Binding SelectedPermissionFilter}" Background="#3D3D5C" Width="180" Margin="0,0,16,0"/>
                            <TextBlock Text="Status:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatusFilter}" Background="#3D3D5C" Width="120"/>
                        </StackPanel>
                        <DataGrid Grid.Row="1" ItemsSource="{Binding AuditLog}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48">
                            <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat='{}{0:dd MMM HH:mm:ss}'}" Width="160"/>
                                <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="*"/>
                                <DataGridTextColumn Header="Permission" Binding="{Binding PermissionName}" Width="180"/>
                                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="100"/>
                                <DataGridTextColumn Header="Processed By" Binding="{Binding ProcessedBy}" Width="120"/>
                                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="200"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </TabItem>
            <TabItem Header="Permission Matrix">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding PermissionMatrix}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Permission" Binding="{Binding PermissionName}" Width="*"/>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                            <DataGridTextColumn Header="Override Count" Binding="{Binding OverrideCount}" Width="120"/>
                            <DataGridTextColumn Header="Approval Rate" Binding="{Binding ApprovalRate, StringFormat='{}{0:N1}%'}" Width="100"/>
                            <DataGridTextColumn Header="Avg Duration" Binding="{Binding AverageDuration}" Width="120"/>
                            <DataGridTextColumn Header="Risk Level" Binding="{Binding RiskLevel}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
        </TabControl>
        <Border Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}" Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"><ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#4F46E5"/><TextBlock Text="Loading..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/></StackPanel>
        </Border>
    </Grid>
</UserControl>
