<UserControl x:Class="HospitalityPOS.WPF.Views.RoleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:HospitalityPOS.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900"
             Background="#1E1E2E">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <converters:InverseBoolConverter x:Key="InverseBool"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="16,8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#4E4E6E"/>
        </Style>

        <Style x:Key="RoleListItem" TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border"
                                Background="#2D2D44"
                                CornerRadius="8"
                                Margin="0,4"
                                Padding="16,12">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3D3D5C"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#6E6EAE"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3D3D5C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Back"
                    Style="{StaticResource ActionButton}"
                    Background="#4E4E6E"
                    Command="{Binding GoBackCommand}"/>

            <TextBlock Grid.Column="1"
                       Text="{Binding Title}"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="2"
                    Content="+ Create Role"
                    Style="{StaticResource ActionButton}"
                    Background="#4CAF50"
                    Command="{Binding CreateRoleCommand}"/>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,16">
            <Button Content="Edit"
                    Style="{StaticResource ActionButton}"
                    Background="#2196F3"
                    Command="{Binding EditRoleCommand}"/>
            <Button Content="Clone"
                    Style="{StaticResource ActionButton}"
                    Background="#FF9800"
                    Command="{Binding CloneRoleCommand}"/>
            <Button Content="Delete"
                    Style="{StaticResource ActionButton}"
                    Background="#F44336"
                    Command="{Binding DeleteRoleCommand}"/>
            <Button Content="Refresh"
                    Style="{StaticResource ActionButton}"
                    Background="#4E4E6E"
                    Command="{Binding LoadRolesCommand}"
                    Margin="24,0,0,0"/>
        </StackPanel>

        <!-- Role List -->
        <Border Grid.Row="2"
                Background="#2D2D44"
                CornerRadius="12"
                Padding="16">
            <ListBox ItemsSource="{Binding Roles}"
                     SelectedItem="{Binding SelectedRole}"
                     Background="Transparent"
                     BorderThickness="0"
                     ItemContainerStyle="{StaticResource RoleListItem}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="White"/>
                                <TextBlock Text="{Binding Description}"
                                           FontSize="13"
                                           Foreground="#8888AA"
                                           Margin="0,4,0,0"/>
                            </StackPanel>

                            <!-- Role Type Badge -->
                            <Border Grid.Column="1"
                                    Background="{Binding IsSystem, Converter={StaticResource BoolToVisibility}}"
                                    CornerRadius="4"
                                    Padding="8,4"
                                    Margin="8,0"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding IsSystem, Converter={StaticResource BoolToVisibility}}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSystem}" Value="True">
                                                <Setter Property="Background" Value="#3D5A80"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="System"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="White"/>
                            </Border>

                            <Border Grid.Column="1"
                                    Background="#2E7D32"
                                    CornerRadius="4"
                                    Padding="8,4"
                                    Margin="8,0"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding IsSystem, Converter={StaticResource InverseBoolToVisibility}}">
                                <TextBlock Text="Custom"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="White"/>
                            </Border>

                            <!-- Permission Count -->
                            <TextBlock Grid.Column="2"
                                       FontSize="13"
                                       Foreground="#6E6E8E"
                                       VerticalAlignment="Center">
                                <Run Text="{Binding RolePermissions.Count, Mode=OneWay}"/>
                                <Run Text=" permissions"/>
                            </TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Loading Indicator -->
        <Border Grid.Row="2"
                Background="#80000000"
                CornerRadius="12"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                             Width="200"
                             Height="4"
                             Foreground="#4CAF50"/>
                <TextBlock Text="{Binding BusyMessage}"
                           Foreground="White"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0"/>
            </StackPanel>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3"
                Background="#2D2D44"
                CornerRadius="8"
                Padding="16,8"
                Margin="0,16,0,0">
            <TextBlock Foreground="#8888AA"
                       FontSize="13">
                <Run Text="Total Roles: "/>
                <Run Text="{Binding Roles.Count, Mode=OneWay}"/>
            </TextBlock>
        </Border>
    </Grid>
</UserControl>
