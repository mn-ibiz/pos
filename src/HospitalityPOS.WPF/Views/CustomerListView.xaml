<UserControl x:Class="HospitalityPOS.WPF.Views.CustomerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:HospitalityPOS.WPF.ViewModels"
             xmlns:converters="clr-namespace:HospitalityPOS.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#1E1E2E">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisConverter" />

        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#2D2D44" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="Margin" Value="0,0,12,0" />
        </Style>

        <Style x:Key="StatLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="#8888AA" />
        </Style>

        <Style x:Key="StatValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="0,4,0,0" />
        </Style>

        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#3D3D5C" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#4A4A6A" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="FilterComboStyle" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Background" Value="#3D3D5C" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#4A4A6A" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="20,12" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#5A5A7A" />
                    <Setter Property="Opacity" Value="0.6" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3D3D5C" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#2D2D44" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="RowBackground" Value="#2D2D44" />
            <Setter Property="AlternatingRowBackground" Value="#363654" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#4A4A6A" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="SelectionUnit" Value="FullRow" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>

        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1E1E2E" />
            <Setter Property="Foreground" Value="#AAAACC" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="BorderBrush" Value="#4A4A6A" />
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#4A4A6A" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding Title}"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="White" />
                <TextBlock Text="Manage your loyalty program members"
                           FontSize="14"
                           Foreground="#8888AA"
                           Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Settings"
                        Command="{Binding NavigateToSettingsCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,12,0" />
                <Button Content="+ Enroll New Customer"
                        Command="{Binding EnrollNewCustomerCommand}"
                        Style="{StaticResource ActionButtonStyle}"
                        Visibility="{Binding CanEnrollCustomers, Converter={StaticResource BoolToVisConverter}}" />
            </StackPanel>
        </Grid>

        <!-- Summary Stats -->
        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="Total Members" Style="{StaticResource StatLabelStyle}" />
                    <TextBlock Text="{Binding TotalMembers, StringFormat=N0}" Style="{StaticResource StatValueStyle}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="Active Members" Style="{StaticResource StatLabelStyle}" />
                    <TextBlock Text="{Binding ActiveMembers, StringFormat=N0}" Style="{StaticResource StatValueStyle}" Foreground="#4CAF50" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="New This Month" Style="{StaticResource StatLabelStyle}" />
                    <TextBlock Text="{Binding NewMembersThisMonth, StringFormat=N0}" Style="{StaticResource StatValueStyle}" Foreground="#6B8DD6" />
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource StatCardStyle}" Margin="0">
                <StackPanel>
                    <TextBlock Text="Total Points Outstanding" Style="{StaticResource StatLabelStyle}" />
                    <TextBlock Text="{Binding TotalPointsOutstanding, StringFormat=N0}" Style="{StaticResource StatValueStyle}" Foreground="#FFA500" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Search and Filters -->
        <Border Grid.Row="2" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,12,0">
                    <TextBox.Resources>
                        <VisualBrush x:Key="SearchHint" TileMode="None" Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <TextBlock Text="Search by name, phone, or membership #..." Foreground="#666688" Margin="4,0,0,0" />
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource SearchTextBoxStyle}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource SearchHint}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding TierOptions}"
                          SelectedItem="{Binding TierFilter}"
                          Style="{StaticResource FilterComboStyle}"
                          Width="140"
                          Margin="0,0,12,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <CheckBox Grid.Column="2"
                          Content="Show Inactive"
                          IsChecked="{Binding ShowInactiveMembers}"
                          Foreground="White"
                          VerticalAlignment="Center"
                          Margin="0,0,12,0" />

                <Button Grid.Column="3"
                        Content="Clear Filters"
                        Command="{Binding ClearFiltersCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,12,0" />

                <Button Grid.Column="4"
                        Content="Export to Excel"
                        Command="{Binding ExportToExcelCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Visibility="{Binding CanExportData, Converter={StaticResource BoolToVisConverter}}" />
            </Grid>
        </Border>

        <!-- Members DataGrid -->
        <Border Grid.Row="3" Background="#2D2D44" CornerRadius="8" Padding="0" ClipToBounds="True">
            <DataGrid ItemsSource="{Binding Members}"
                      SelectedItem="{Binding SelectedMember}"
                      Style="{StaticResource DataGridStyle}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      CellStyle="{StaticResource DataGridCellStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="MEMBERSHIP #" Binding="{Binding MembershipNumber}" Width="130" />
                    <DataGridTextColumn Header="NAME" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="PHONE" Binding="{Binding PhoneNumber}" Width="130" />
                    <DataGridTemplateColumn Header="TIER" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Tier}" Value="Bronze">
                                                    <Setter Property="Background" Value="#4A3A2A" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Tier}" Value="Silver">
                                                    <Setter Property="Background" Value="#4A4A5A" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Tier}" Value="Gold">
                                                    <Setter Property="Background" Value="#5A4A2A" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Tier}" Value="Platinum">
                                                    <Setter Property="Background" Value="#3A4A5A" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Tier}" FontSize="11" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Tier}" Value="Bronze">
                                                        <Setter Property="Foreground" Value="#CD7F32" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Tier}" Value="Silver">
                                                        <Setter Property="Foreground" Value="#C0C0C0" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Tier}" Value="Gold">
                                                        <Setter Property="Foreground" Value="#FFD700" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Tier}" Value="Platinum">
                                                        <Setter Property="Foreground" Value="#E5E4E2" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="POINTS" Binding="{Binding PointsBalance, StringFormat=N0}" Width="90" />
                    <DataGridTextColumn Header="TOTAL SPEND" Binding="{Binding TotalSpend, StringFormat=KES {0:N0}}" Width="120" />
                    <DataGridTextColumn Header="VISITS" Binding="{Binding VisitCount}" Width="70" />
                    <DataGridTextColumn Header="LAST VISIT" Binding="{Binding LastVisitAt, StringFormat=d}" Width="100" />
                    <DataGridTemplateColumn Header="STATUS" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock FontSize="11" FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Active" />
                                            <Setter Property="Foreground" Value="#4CAF50" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Text" Value="Inactive" />
                                                    <Setter Property="Foreground" Value="#FF6B6B" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Action Bar -->
        <Border Grid.Row="4" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,16,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           VerticalAlignment="Center"
                           Foreground="#8888AA"
                           FontSize="13">
                    <Run Text="Showing " />
                    <Run Text="{Binding Members.Count, Mode=OneWay}" FontWeight="SemiBold" Foreground="White" />
                    <Run Text=" members" />
                </TextBlock>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="View Details"
                            Command="{Binding ViewMemberDetailsCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,8,0"
                            IsEnabled="{Binding SelectedMember, Converter={StaticResource BoolToVisConverter}}" />
                    <Button Content="Transaction History"
                            Command="{Binding ViewTransactionHistoryCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,8,0"
                            IsEnabled="{Binding SelectedMember, Converter={StaticResource BoolToVisConverter}}" />
                    <Button Command="{Binding DeactivateMemberCommand}"
                            Margin="0,0,8,0"
                            Visibility="{Binding CanDeactivateMembers, Converter={StaticResource BoolToVisConverter}}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                <Setter Property="Content" Value="Deactivate" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedMember.IsActive}" Value="False">
                                        <Setter Property="Content" Value="Reactivate" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="5"
                Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisConverter}}"
                Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                             Width="200"
                             Height="4"
                             Foreground="#6B8DD6" />
                <TextBlock Text="{Binding BusyMessage}"
                           Foreground="White"
                           FontSize="16"
                           Margin="0,16,0,0"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Error Message -->
        <Border Grid.Row="4"
                Background="#3D1A1A"
                CornerRadius="8"
                Padding="16"
                Margin="0,16,0,0"
                Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVisConverter}}"
                Panel.ZIndex="50">
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#FF6B6B"
                       FontSize="14"
                       TextWrapping="Wrap" />
        </Border>
    </Grid>
</UserControl>
