<UserControl x:Class="HospitalityPOS.WPF.Views.EmployeeEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#1E1E2E">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#3D3D5C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Background="#3D3D5C"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="16,12"
                    FontSize="14"
                    Cursor="Hand"/>

            <TextBlock Grid.Column="1"
                       Text="{Binding Title}"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Personal Information -->
                <Border Background="#2D2D44" CornerRadius="12" Padding="20" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="PERSONAL INFORMATION" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,20"/>

                        <TextBlock Text="Employee Number" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding EmployeeNumber}" Style="{StaticResource FormTextBox}" IsReadOnly="True"/>

                        <TextBlock Text="First Name *" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding FirstName}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Last Name *" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding LastName}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="National ID" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding NationalId}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Phone" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding Phone}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Email" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding Email}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Address" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding Address}" Style="{StaticResource FormTextBox}" TextWrapping="Wrap" Height="60"/>

                        <TextBlock Text="Date of Birth" Style="{StaticResource FormLabel}"/>
                        <DatePicker SelectedDate="{Binding DateOfBirth}" Background="#3D3D5C" Foreground="White" Margin="0,0,0,16"/>
                    </StackPanel>
                </Border>

                <!-- Employment Information -->
                <Border Grid.Column="1" Background="#2D2D44" CornerRadius="12" Padding="20" Margin="4,0">
                    <StackPanel>
                        <TextBlock Text="EMPLOYMENT DETAILS" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,20"/>

                        <TextBlock Text="Hire Date *" Style="{StaticResource FormLabel}"/>
                        <DatePicker SelectedDate="{Binding HireDate}" Background="#3D3D5C" Foreground="White" Margin="0,0,0,16"/>

                        <TextBlock Text="Department" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding Department}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Position" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding Position}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Employment Type" Style="{StaticResource FormLabel}"/>
                        <ComboBox ItemsSource="{Binding EmploymentTypes}"
                                  SelectedItem="{Binding EmploymentType}"
                                  Background="#3D3D5C"
                                  Foreground="White"
                                  Margin="0,0,0,16"/>

                        <TextBlock Text="Pay Frequency" Style="{StaticResource FormLabel}"/>
                        <ComboBox ItemsSource="{Binding PayFrequencies}"
                                  SelectedItem="{Binding PayFrequency}"
                                  Background="#3D3D5C"
                                  Foreground="White"
                                  Margin="0,0,0,16"/>

                        <TextBlock Text="Link to User Account (Optional)" Style="{StaticResource FormLabel}"/>
                        <ComboBox ItemsSource="{Binding Users}"
                                  SelectedItem="{Binding SelectedUser}"
                                  DisplayMemberPath="Username"
                                  Background="#3D3D5C"
                                  Foreground="White"
                                  Margin="0,0,0,16"/>
                    </StackPanel>
                </Border>

                <!-- Salary & Banking -->
                <Border Grid.Column="2" Background="#2D2D44" CornerRadius="12" Padding="20" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="SALARY &amp; BANKING" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,20"/>

                        <TextBlock Text="Basic Salary (KSh)" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding BasicSalary}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Bank Name" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding BankName}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Bank Account Number" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding BankAccountNumber}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="STATUTORY IDs" FontSize="14" FontWeight="SemiBold" Foreground="White" Margin="0,20,0,16"/>

                        <TextBlock Text="KRA PIN" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding TaxId}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="NSSF Number" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding NssfNumber}" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="NHIF Number" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="{Binding NhifNumber}" Style="{StaticResource FormTextBox}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- Save Button -->
        <Border Grid.Row="2" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Save Employee"
                        Command="{Binding SaveCommand}"
                        Background="#4F46E5"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="32,12"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Cursor="Hand"/>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#4F46E5"/>
                <TextBlock Text="Saving..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
