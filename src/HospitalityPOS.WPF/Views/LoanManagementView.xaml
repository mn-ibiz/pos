<UserControl x:Class="HospitalityPOS.WPF.Views.LoanManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1400" Background="#1E1E2E">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="Back" Command="{Binding GoBackCommand}" Background="#3D3D5C" Foreground="White" BorderThickness="0" Padding="16,12" Cursor="Hand"/>
            <TextBlock Grid.Column="1" Text="Loan Management" FontSize="28" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button Grid.Column="2" Content="+ New Application" Command="{Binding OpenApplicationFormCommand}" Background="#4F46E5" Foreground="White" BorderThickness="0" Padding="24,12" Cursor="Hand"/>
        </Grid>

        <!-- Summary Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Total Disbursed" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalDisbursed, StringFormat='KSh {0:N0}'}" FontSize="24" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Outstanding" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalOutstanding, StringFormat='KSh {0:N0}'}" FontSize="24" FontWeight="Bold" Foreground="#FF9800" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Pending Applications" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding PendingApplicationsCount}" FontSize="24" FontWeight="Bold" Foreground="#2196F3" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="3" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Active Loans" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding ActiveLoansCount}" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Tab Control -->
        <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedTabIndex}" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="#2D2D44"/>
                    <Setter Property="Foreground" Value="#9CA3AF"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Background="#2D2D44" CornerRadius="8,8,0,0" Margin="0,0,4,0">
                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="20,12"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#4F46E5"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabItem Header="Pending Applications">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding PendingApplications}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Employee" Binding="{Binding EmployeeName}" Width="*"/>
                            <DataGridTextColumn Header="Amount" Binding="{Binding RequestedAmount, StringFormat='KSh {0:N0}'}" Width="120"/>
                            <DataGridTextColumn Header="Term" Binding="{Binding RequestedTermMonths, StringFormat='{}{0} months'}" Width="100"/>
                            <DataGridTextColumn Header="Purpose" Binding="{Binding Purpose}" Width="150"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding ApplicationDate, StringFormat='{}{0:dd MMM}'}" Width="100"/>
                            <DataGridTemplateColumn Header="Actions" Width="220">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Approve" Command="{Binding DataContext.ApproveApplicationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#059669" Foreground="White" Padding="10,6" Margin="0,0,4,0" BorderThickness="0" Cursor="Hand"/>
                                            <Button Content="Reject" Command="{Binding DataContext.RejectApplicationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#DC2626" Foreground="White" Padding="10,6" Margin="0,0,4,0" BorderThickness="0" Cursor="Hand"/>
                                            <Button Content="Disburse" Command="{Binding DataContext.DisburseCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#4F46E5" Foreground="White" Padding="10,6" BorderThickness="0" Cursor="Hand"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

            <TabItem Header="Active Loans">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding ActiveLoans}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Employee" Binding="{Binding EmployeeName}" Width="*"/>
                            <DataGridTextColumn Header="Principal" Binding="{Binding PrincipalAmount, StringFormat='KSh {0:N0}'}" Width="100"/>
                            <DataGridTextColumn Header="Outstanding" Binding="{Binding OutstandingBalance, StringFormat='KSh {0:N0}'}" Width="100"/>
                            <DataGridTextColumn Header="Monthly" Binding="{Binding MonthlyInstallment, StringFormat='KSh {0:N0}'}" Width="100"/>
                            <DataGridTextColumn Header="Rate" Binding="{Binding InterestRate, StringFormat='{}{0}%'}" Width="60"/>
                            <DataGridTextColumn Header="Maturity" Binding="{Binding MaturityDate, StringFormat='{}{0:dd MMM yy}'}" Width="100"/>
                            <DataGridTemplateColumn Header="Actions" Width="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Schedule" Command="{Binding DataContext.ViewRepaymentScheduleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#4F46E5" Foreground="White" Padding="10,6" Margin="0,0,4,0" BorderThickness="0" Cursor="Hand"/>
                                            <Button Content="Repay" Command="{Binding DataContext.RecordRepaymentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#059669" Foreground="White" Padding="10,6" BorderThickness="0" Cursor="Hand"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

            <TabItem Header="Settings">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8" Padding="24">
                    <StackPanel MaxWidth="600">
                        <TextBlock Text="Loan Settings" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,24"/>
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Maximum Loan Amount (KSh)" Foreground="#9CA3AF" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding Settings.MaxLoanAmount}" Background="#3D3D5C" Foreground="White" Padding="12" BorderThickness="0"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Default Interest Rate (%)" Foreground="#9CA3AF" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding Settings.DefaultInterestRate}" Background="#3D3D5C" Foreground="White" Padding="12" BorderThickness="0"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Maximum Term (Months)" Foreground="#9CA3AF" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding Settings.MaxTermMonths}" Background="#3D3D5C" Foreground="White" Padding="12" BorderThickness="0"/>
                        </StackPanel>
                        <CheckBox Content="Require Manager Approval" IsChecked="{Binding Settings.RequireManagerApproval}" Foreground="White" Margin="0,0,0,16"/>
                        <CheckBox Content="Allow Multiple Loans" IsChecked="{Binding Settings.AllowMultipleLoans}" Foreground="White" Margin="0,0,0,24"/>
                        <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Background="#4F46E5" Foreground="White" BorderThickness="0" Padding="24,12" HorizontalAlignment="Left" Cursor="Hand"/>
                    </StackPanel>
                </Border>
            </TabItem>
        </TabControl>

        <Border Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}" Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#4F46E5"/>
                <TextBlock Text="Loading..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
