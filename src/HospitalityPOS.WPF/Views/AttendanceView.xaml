<UserControl x:Class="HospitalityPOS.WPF.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#1E1E2E">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Back"
                    Command="{Binding GoBackCommand}"
                    Background="#3D3D5C"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="16,12"
                    FontSize="14"
                    Cursor="Hand"/>

            <TextBlock Grid.Column="1"
                       Text="{Binding Title}"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>

        <!-- Quick Actions -->
        <Border Grid.Row="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Employee:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding Employees}"
                              SelectedItem="{Binding SelectedEmployee}"
                              DisplayMemberPath="FullName"
                              Width="250"
                              Background="#3D3D5C"
                              Foreground="White"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Today's Status:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" FontSize="16">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Not Clocked In"/>
                                <Setter Property="Foreground" Value="#F44336"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsClockedIn}" Value="True">
                                        <Setter Property="Text" Value="Clocked In"/>
                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Clock In"
                            Command="{Binding ClockInCommand}"
                            Background="#4CAF50"
                            Foreground="White"
                            BorderThickness="0"
                            Padding="16,8"
                            FontSize="14"
                            Cursor="Hand"
                            Margin="0,0,8,0"
                            IsEnabled="{Binding IsClockedIn, Converter={StaticResource BoolToVisConverter}, ConverterParameter=Invert}"/>
                    <Button Content="Clock Out"
                            Command="{Binding ClockOutCommand}"
                            Background="#F44336"
                            Foreground="White"
                            BorderThickness="0"
                            Padding="16,8"
                            FontSize="14"
                            Cursor="Hand"
                            Margin="0,0,8,0"/>
                    <Button Content="Start Break"
                            Command="{Binding StartBreakCommand}"
                            Background="#FF9800"
                            Foreground="White"
                            BorderThickness="0"
                            Padding="16,8"
                            FontSize="14"
                            Cursor="Hand"
                            Margin="0,0,8,0"/>
                    <Button Content="End Break"
                            Command="{Binding EndBreakCommand}"
                            Background="#2196F3"
                            Foreground="White"
                            BorderThickness="0"
                            Padding="16,8"
                            FontSize="14"
                            Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Date Range Filter -->
        <Border Grid.Row="2" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="Start Date:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker SelectedDate="{Binding StartDate}" Width="140" Background="#3D3D5C" Foreground="White"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="End Date:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker SelectedDate="{Binding EndDate}" Width="140" Background="#3D3D5C" Foreground="White"/>
                </StackPanel>

                <Button Grid.Column="2"
                        Content="Load Attendance"
                        Command="{Binding LoadAttendanceCommand}"
                        Background="#4F46E5"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="16,8"
                        FontSize="14"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Attendance Records -->
        <Border Grid.Row="3" Background="#2D2D44" CornerRadius="12">
            <Grid>
                <Border Background="#80000000"
                        CornerRadius="12"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"
                        Panel.ZIndex="10">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#4F46E5"/>
                        <TextBlock Text="Loading..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <DataGrid ItemsSource="{Binding AttendanceRecords}"
                          SelectedItem="{Binding SelectedRecord}"
                          AutoGenerateColumns="False"
                          Background="Transparent"
                          Foreground="White"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#3D3D5C"
                          RowBackground="#2D2D44"
                          AlternatingRowBackground="#353550"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          FontSize="14"
                          RowHeight="48"
                          IsReadOnly="True">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#3D3D5C"/>
                            <Setter Property="Foreground" Value="#AAAACC"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Padding" Value="16,12"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="16,0"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#4A4A6A"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding AttendanceDate, StringFormat='dd/MM/yyyy'}" Width="100"/>
                        <DataGridTextColumn Header="Clock In" Binding="{Binding ClockIn, StringFormat='{}{0:hh\\:mm}'}" Width="100"/>
                        <DataGridTextColumn Header="Clock Out" Binding="{Binding ClockOut, StringFormat='{}{0:hh\\:mm}'}" Width="100"/>
                        <DataGridTextColumn Header="Break Start" Binding="{Binding BreakStart, StringFormat='{}{0:hh\\:mm}'}" Width="100"/>
                        <DataGridTextColumn Header="Break End" Binding="{Binding BreakEnd, StringFormat='{}{0:hh\\:mm}'}" Width="100"/>
                        <DataGridTextColumn Header="Hours" Binding="{Binding HoursWorked, StringFormat='{0:N1}'}" Width="80"/>
                        <DataGridTextColumn Header="Overtime" Binding="{Binding OvertimeHours, StringFormat='{0:N1}'}" Width="80"/>
                        <DataGridTemplateColumn Header="Status" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="8,4">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#4CAF50"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Absent">
                                                        <Setter Property="Background" Value="#F44336"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Late">
                                                        <Setter Property="Background" Value="#FF9800"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="OnLeave">
                                                        <Setter Property="Background" Value="#2196F3"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Status}" Foreground="White" FontSize="12" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Summary -->
        <Border Grid.Row="4" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,32,0">
                    <TextBlock Text="Present:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding Summaries[0].PresentDays}" FontWeight="Bold" Foreground="#4CAF50" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,32,0">
                    <TextBlock Text="Absent:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding Summaries[0].AbsentDays}" FontWeight="Bold" Foreground="#F44336" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,32,0">
                    <TextBlock Text="Late:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding Summaries[0].LateDays}" FontWeight="Bold" Foreground="#FF9800" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,32,0">
                    <TextBlock Text="Total Hours:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding Summaries[0].TotalHoursWorked, StringFormat='{0:N1}'}" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Overtime:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding Summaries[0].TotalOvertimeHours, StringFormat='{0:N1}'}" FontWeight="Bold" Foreground="#7C3AED" VerticalAlignment="Center"/>
                </StackPanel>

                <Button Content="Add Manual Entry"
                        Command="{Binding AddManualEntryCommand}"
                        Background="#3D3D5C"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="16,8"
                        FontSize="14"
                        Cursor="Hand"
                        Margin="32,0,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
