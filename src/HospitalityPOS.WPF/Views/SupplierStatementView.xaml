<UserControl x:Class="HospitalityPOS.WPF.Views.SupplierStatementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#1E1E2E">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="Back"
                    Command="{Binding GoBackCommand}"
                    Background="#3D3D5C"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="16,12"
                    FontSize="14"
                    Cursor="Hand"/>

            <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="Export PDF"
                        Command="{Binding ExportToPdfCommand}"
                        Background="#059669"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="16,12"
                        FontSize="14"
                        Cursor="Hand"
                        Margin="0,0,8,0"/>
                <Button Content="Export CSV"
                        Command="{Binding ExportToCsvCommand}"
                        Background="#0284C7"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="16,12"
                        FontSize="14"
                        Cursor="Hand"
                        Margin="0,0,8,0"/>
                <Button Content="Print"
                        Command="{Binding PrintStatementCommand}"
                        Background="#6B7280"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="16,12"
                        FontSize="14"
                        Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <!-- Date Range Filter -->
        <Border Grid.Row="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="Start Date:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker SelectedDate="{Binding StartDate}"
                                Width="140"
                                Background="#3D3D5C"
                                Foreground="White"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="End Date:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker SelectedDate="{Binding EndDate}"
                                Width="140"
                                Background="#3D3D5C"
                                Foreground="White"/>
                </StackPanel>

                <Button Grid.Column="2"
                        Content="Generate Statement"
                        Command="{Binding GenerateStatementCommand}"
                        Background="#4F46E5"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="16,8"
                        FontSize="14"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Summary Cards -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Opening Balance" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Statement.OpeningBalance, StringFormat='KSh {0:N2}'}"
                               FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Total Invoices" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Statement.TotalInvoices, StringFormat='KSh {0:N2}'}"
                               FontSize="20" FontWeight="Bold" Foreground="#F44336" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Total Payments" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Statement.TotalPayments, StringFormat='KSh {0:N2}'}"
                               FontSize="20" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Closing Balance" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Statement.ClosingBalance, StringFormat='KSh {0:N2}'}"
                               FontSize="20" FontWeight="Bold" Foreground="#FF9800" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Statement Lines -->
        <Border Grid.Row="3" Background="#2D2D44" CornerRadius="12">
            <Grid>
                <Border Background="#80000000"
                        CornerRadius="12"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"
                        Panel.ZIndex="10">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#4F46E5"/>
                        <TextBlock Text="Generating statement..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <DataGrid ItemsSource="{Binding StatementLines}"
                          AutoGenerateColumns="False"
                          Background="Transparent"
                          Foreground="White"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#3D3D5C"
                          RowBackground="#2D2D44"
                          AlternatingRowBackground="#353550"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          FontSize="14"
                          RowHeight="44"
                          IsReadOnly="True">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#3D3D5C"/>
                            <Setter Property="Foreground" Value="#AAAACC"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Padding" Value="16,12"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="16,0"/>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat='dd/MM/yyyy'}" Width="100"/>
                        <DataGridTextColumn Header="Reference" Binding="{Binding Reference}" Width="120"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                        <DataGridTemplateColumn Header="Debit" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Debit, StringFormat='KSh {0:N2}'}"
                                               Foreground="#F44336"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding Debit, Converter={StaticResource BoolToVisConverter}}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Credit" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Credit, StringFormat='KSh {0:N2}'}"
                                               Foreground="#4CAF50"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding Credit, Converter={StaticResource BoolToVisConverter}}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Balance" Binding="{Binding RunningBalance, StringFormat='KSh {0:N2}'}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Aging Summary -->
        <Border Grid.Row="4" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,16,0,0"
                Visibility="{Binding AgingSummary, Converter={StaticResource BoolToVisConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Aging:" Foreground="White" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center" Margin="0,0,20,0"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <StackPanel Margin="0,0,32,0" HorizontalAlignment="Center">
                        <TextBlock Text="Current" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding AgingSummary.Current, StringFormat='KSh {0:N0}'}"
                                   FontSize="16" FontWeight="SemiBold" Foreground="#4CAF50" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,32,0" HorizontalAlignment="Center">
                        <TextBlock Text="31-60 Days" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding AgingSummary.Days30, StringFormat='KSh {0:N0}'}"
                                   FontSize="16" FontWeight="SemiBold" Foreground="#FFC107" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,32,0" HorizontalAlignment="Center">
                        <TextBlock Text="61-90 Days" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding AgingSummary.Days60, StringFormat='KSh {0:N0}'}"
                                   FontSize="16" FontWeight="SemiBold" Foreground="#FF9800" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="90+ Days" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding AgingSummary.Days90Plus, StringFormat='KSh {0:N0}'}"
                                   FontSize="16" FontWeight="SemiBold" Foreground="#F44336" HorizontalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
