<UserControl x:Class="HospitalityPOS.WPF.Views.LoginAuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:HospitalityPOS.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:LoginAuditViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#1E1E2E">

    <UserControl.Resources>
        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3D3D5C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4D4D7C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="#2D2D44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4A4A6A"/>
            <Setter Property="Height" Value="36"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2D2D44" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="&#xE72B;"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="16"
                        Command="{Binding GoBackCommand}"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        VerticalAlignment="Center"
                        Margin="0,0,16,0"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Login Audit Trail"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="White"/>
                    <TextBlock Text="Track login attempts and security events"
                               FontSize="12"
                               Foreground="#8888AA"/>
                </StackPanel>

                <Button Grid.Column="2"
                        Content="Refresh"
                        Command="{Binding LoadDataCommand}"
                        Style="{StaticResource FilterButtonStyle}"
                        Background="#4CAF50"/>
            </Grid>
        </Border>

        <!-- Suspicious Activity Warning -->
        <Border Grid.Row="1"
                Background="#3D2D2D"
                BorderBrush="#FF6B6B"
                BorderThickness="1"
                CornerRadius="6"
                Margin="24,16,24,0"
                Padding="16"
                Visibility="{Binding HasSuspiciousActivity, Converter={StaticResource BoolToVisibility}}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="&#x26A0;"
                               Foreground="#FF6B6B"
                               FontSize="18"
                               Margin="0,0,8,0"/>
                    <TextBlock Text="SUSPICIOUS ACTIVITY DETECTED"
                               Foreground="#FF6B6B"
                               FontWeight="Bold"
                               FontSize="14"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <ItemsControl ItemsSource="{Binding SuspiciousActivity}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Foreground="#CCCCCC"
                                           FontSize="13">
                                    <Run Text="{Binding Username}"/>
                                    <Run Text=" - "/>
                                    <Run Text="{Binding TimeWindow}"/>
                                </TextBlock>
                                <Border Grid.Column="1"
                                        Background="#5C1D1D"
                                        CornerRadius="4"
                                        Padding="8,2">
                                    <TextBlock Foreground="#FF6B6B"
                                               FontWeight="Bold"
                                               FontSize="12">
                                        <Run Text="{Binding FailedAttempts}"/>
                                        <Run Text=" failed attempts"/>
                                    </TextBlock>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Filters -->
        <Border Grid.Row="2" Background="#252538" Padding="24,16" Margin="0,16,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- User Filter -->
                <StackPanel Grid.Column="0" Margin="0,0,24,0">
                    <TextBlock Text="User" Foreground="#8888AA" FontSize="11" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Users}"
                              SelectedItem="{Binding SelectedUser}"
                              DisplayMemberPath="DisplayName"
                              Width="180"
                              Background="#2D2D44"
                              Foreground="White"
                              BorderBrush="#4A4A6A"/>
                </StackPanel>

                <!-- From Date -->
                <StackPanel Grid.Column="1" Margin="0,0,16,0">
                    <TextBlock Text="From" Foreground="#8888AA" FontSize="11" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding FromDate}"
                                Style="{StaticResource DatePickerStyle}"
                                Width="140"/>
                </StackPanel>

                <!-- To Date -->
                <StackPanel Grid.Column="2" Margin="0,0,24,0">
                    <TextBlock Text="To" Foreground="#8888AA" FontSize="11" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding ToDate}"
                                Style="{StaticResource DatePickerStyle}"
                                Width="140"/>
                </StackPanel>

                <!-- Date Presets -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="Today" Command="{Binding SetTodayCommand}" Style="{StaticResource FilterButtonStyle}" Margin="0,0,8,0"/>
                    <Button Content="Yesterday" Command="{Binding SetYesterdayCommand}" Style="{StaticResource FilterButtonStyle}" Margin="0,0,8,0"/>
                    <Button Content="This Week" Command="{Binding SetThisWeekCommand}" Style="{StaticResource FilterButtonStyle}" Margin="0,0,8,0"/>
                    <Button Content="This Month" Command="{Binding SetThisMonthCommand}" Style="{StaticResource FilterButtonStyle}"/>
                </StackPanel>

                <!-- Status Filter -->
                <StackPanel Grid.Column="5" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="All" Command="{Binding ShowAllCommand}" Style="{StaticResource FilterButtonStyle}" Margin="0,0,8,0"/>
                    <Button Content="Success" Command="{Binding ShowSuccessOnlyCommand}" Style="{StaticResource FilterButtonStyle}" Background="#1A3D1A" Margin="0,0,8,0"/>
                    <Button Content="Failed" Command="{Binding ShowFailedOnlyCommand}" Style="{StaticResource FilterButtonStyle}" Background="#3D1A1A"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Data Grid -->
        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding AuditRecords}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Background="#1E1E2E"
                  RowBackground="#252538"
                  AlternatingRowBackground="#2D2D44"
                  Foreground="White"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#3D3D5C"
                  BorderThickness="0"
                  Margin="24,16,24,24"
                  HeadersVisibility="Column">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#3D3D5C"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="12,10"/>
                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                    <Setter Property="BorderBrush" Value="#4A4A6A"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="12,10"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#4A4A6A"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="TIMESTAMP" Binding="{Binding TimestampDisplay}" Width="160"/>
                <DataGridTextColumn Header="USER" Binding="{Binding FullName}" Width="150"/>
                <DataGridTextColumn Header="USERNAME" Binding="{Binding Username}" Width="120"/>

                <DataGridTemplateColumn Header="STATUS" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding StatusColor}"
                                    CornerRadius="4"
                                    Padding="8,4"
                                    HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Status}"
                                           Foreground="White"
                                           FontWeight="SemiBold"
                                           FontSize="11"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="REASON" Binding="{Binding FailureReason}" Width="150"/>
                <DataGridTextColumn Header="MACHINE" Binding="{Binding MachineName}" Width="120"/>
                <DataGridTextColumn Header="IP ADDRESS" Binding="{Binding IpAddress}" Width="120"/>
                <DataGridTextColumn Header="SESSION" Binding="{Binding SessionDurationDisplay}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="4"
                Background="#99000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Background="#3D3D5C" Foreground="#4CAF50"/>
                <TextBlock Text="{Binding BusyMessage}"
                           Foreground="White"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
