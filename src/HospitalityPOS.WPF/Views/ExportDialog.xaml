<Window x:Class="HospitalityPOS.WPF.Views.ExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:HospitalityPOS.WPF.ViewModels"
        xmlns:enums="clr-namespace:HospitalityPOS.Core.Enums;assembly=HospitalityPOS.Core"
        mc:Ignorable="d"
        Title="Export Report"
        Height="420"
        Width="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance Type=viewmodels:ExportDialogViewModel}">

    <Window.Resources>
        <!-- Format radio button style -->
        <Style x:Key="FormatRadioStyle" TargetType="RadioButton">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <!-- Primary button style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#106EBE"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary button style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0078D4" Padding="20,16">
            <StackPanel>
                <TextBlock Text="Export Report"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="White"/>
                <TextBlock Text="{Binding ReportName}"
                           FontSize="14"
                           Foreground="#E0E0E0"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- Report Info -->
                <Border Background="White" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Report:"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,16,8"/>
                        <TextBlock Grid.Row="0" Grid.Column="1"
                                   Text="{Binding ReportName}"
                                   FontSize="14"
                                   Margin="0,0,0,8"/>

                        <TextBlock Grid.Row="1" Grid.Column="0"
                                   Text="Date Range:"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,16,0"/>
                        <TextBlock Grid.Row="1" Grid.Column="1"
                                   Text="{Binding DateRange}"
                                   FontSize="14"/>
                    </Grid>
                </Border>

                <!-- Export Format -->
                <Border Background="White" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Export Format"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,12"/>

                        <RadioButton x:Name="CsvRadio"
                                     Content="CSV (Excel/Spreadsheet)"
                                     IsChecked="{Binding SelectedFormat, Converter={StaticResource EnumBooleanConverter}, ConverterParameter={x:Static enums:ExportFormat.CSV}}"
                                     Style="{StaticResource FormatRadioStyle}"
                                     GroupName="ExportFormat"/>

                        <RadioButton x:Name="PdfRadio"
                                     Content="PDF Document (Not available)"
                                     IsChecked="{Binding SelectedFormat, Converter={StaticResource EnumBooleanConverter}, ConverterParameter={x:Static enums:ExportFormat.PDF}}"
                                     Style="{StaticResource FormatRadioStyle}"
                                     GroupName="ExportFormat"
                                     IsEnabled="{Binding IsPdfSupported}"/>
                    </StackPanel>
                </Border>

                <!-- File Location -->
                <Border Background="White" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Save As"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,12"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0"
                                     Text="{Binding FilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     FontSize="13"
                                     Padding="8"
                                     VerticalContentAlignment="Center"
                                     IsReadOnly="True"/>

                            <Button Grid.Column="1"
                                    Content="Browse..."
                                    Command="{Binding BrowseCommand}"
                                    Margin="8,0,0,0"
                                    Padding="16,8"
                                    FontSize="13"/>
                        </Grid>

                        <TextBlock Text="{Binding FileName}"
                                   FontSize="12"
                                   Foreground="#666666"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Error Message -->
                <Border Background="#FFE0E0"
                        CornerRadius="4"
                        Padding="12"
                        Margin="0,0,0,16"
                        Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}">
                    <TextBlock Text="{Binding ErrorMessage}"
                               FontSize="13"
                               Foreground="#CC0000"
                               TextWrapping="Wrap"/>
                </Border>

                <!-- Exporting Progress -->
                <Border Background="White"
                        CornerRadius="4"
                        Padding="16"
                        Visibility="{Binding IsExporting, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True"
                                     Width="200"
                                     Height="4"/>
                        <TextBlock Text="Exporting report..."
                                   FontSize="13"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <Border Grid.Row="2" Background="White" Padding="20,16" BorderThickness="0,1,0,0" BorderBrush="#E0E0E0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1"
                        Content="Cancel"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        IsEnabled="{Binding IsExporting, Converter={StaticResource InverseBoolConverter}}"
                        Margin="0,0,12,0"/>

                <Button Grid.Column="2"
                        Content="Export"
                        Command="{Binding ExportCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        IsEnabled="{Binding IsExporting, Converter={StaticResource InverseBoolConverter}}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
