<UserControl x:Class="HospitalityPOS.WPF.Views.LeaveManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1400"
             Background="#1E1E2E">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="Back" Command="{Binding GoBackCommand}"
                    Background="#3D3D5C" Foreground="White" BorderThickness="0" Padding="16,12" Cursor="Hand"/>

            <TextBlock Grid.Column="1" Text="Leave Management" FontSize="28" FontWeight="SemiBold"
                       Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="+ New Request" Command="{Binding OpenRequestFormCommand}"
                        Background="#4F46E5" Foreground="White" BorderThickness="0" Padding="24,12" Margin="0,0,8,0" Cursor="Hand"/>
                <Button Content="+ Leave Type" Command="{Binding CreateLeaveTypeCommand}"
                        Background="#059669" Foreground="White" BorderThickness="0" Padding="24,12" Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <!-- Summary Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Pending Requests" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding PendingRequestsCount}" FontSize="24" FontWeight="Bold" Foreground="#FF9800" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Approved This Month" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding ApprovedThisMonth}" FontSize="24" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="On Leave Today" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding OnLeaveToday}" FontSize="24" FontWeight="Bold" Foreground="#2196F3" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Leave Types" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding ActiveLeaveTypes}" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Tab Control -->
        <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedTabIndex}" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="#2D2D44"/>
                    <Setter Property="Foreground" Value="#9CA3AF"/>
                    <Setter Property="Padding" Value="20,12"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Background="#2D2D44" CornerRadius="8,8,0,0" Margin="0,0,4,0">
                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center"
                                                      ContentSource="Header" Margin="20,12"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#4F46E5"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Pending Requests Tab -->
            <TabItem Header="Pending Requests">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding PendingRequests}" SelectedItem="{Binding SelectedRequest}"
                              AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0"
                              GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#3D3D5C"
                              RowBackground="#2D2D44" AlternatingRowBackground="#353550"
                              SelectionMode="Single" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#3D3D5C"/>
                                <Setter Property="Foreground" Value="#AAAACC"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Employee" Binding="{Binding EmployeeName}" Width="*"/>
                            <DataGridTextColumn Header="Leave Type" Binding="{Binding LeaveTypeName}" Width="120"/>
                            <DataGridTextColumn Header="From" Binding="{Binding StartDate, StringFormat='{}{0:dd MMM}'}" Width="100"/>
                            <DataGridTextColumn Header="To" Binding="{Binding EndDate, StringFormat='{}{0:dd MMM}'}" Width="100"/>
                            <DataGridTextColumn Header="Days" Binding="{Binding Days}" Width="60"/>
                            <DataGridTemplateColumn Header="Actions" Width="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Approve" Command="{Binding DataContext.ApproveRequestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}" Background="#059669" Foreground="White" BorderThickness="0" Padding="12,6" Margin="0,0,4,0" Cursor="Hand"/>
                                            <Button Content="Reject" Command="{Binding DataContext.RejectRequestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}" Background="#DC2626" Foreground="White" BorderThickness="0" Padding="12,6" Cursor="Hand"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

            <!-- Leave Types Tab -->
            <TabItem Header="Leave Types">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding LeaveTypes}" SelectedItem="{Binding SelectedLeaveType}"
                              AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0"
                              RowBackground="#2D2D44" AlternatingRowBackground="#353550"
                              SelectionMode="Single" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#3D3D5C"/>
                                <Setter Property="Foreground" Value="#AAAACC"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Default Days" Binding="{Binding DefaultDays}" Width="100"/>
                            <DataGridCheckBoxColumn Header="Paid" Binding="{Binding IsPaid}" Width="60"/>
                            <DataGridCheckBoxColumn Header="Approval Required" Binding="{Binding RequiresApproval}" Width="120"/>
                            <DataGridCheckBoxColumn Header="Carry Over" Binding="{Binding AllowCarryOver}" Width="100"/>
                            <DataGridTemplateColumn Header="Actions" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Edit" Command="{Binding DataContext.EditLeaveTypeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}" Background="#4F46E5" Foreground="White" BorderThickness="0" Padding="12,6" Margin="0,0,4,0" Cursor="Hand"/>
                                            <Button Content="Deactivate" Command="{Binding DataContext.DeactivateLeaveTypeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}" Background="#DC2626" Foreground="White" BorderThickness="0" Padding="12,6" Cursor="Hand"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

            <!-- Settings Tab -->
            <TabItem Header="Settings">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8" Padding="24">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel MaxWidth="600">
                            <TextBlock Text="Leave Settings" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,24"/>

                            <CheckBox Content="Require Manager Approval" IsChecked="{Binding Settings.RequireManagerApproval}" Foreground="White" Margin="0,0,0,16"/>
                            <CheckBox Content="Allow Negative Balance" IsChecked="{Binding Settings.AllowNegativeBalance}" Foreground="White" Margin="0,0,0,16"/>
                            <CheckBox Content="Send Email Notifications" IsChecked="{Binding Settings.SendEmailNotifications}" Foreground="White" Margin="0,0,0,16"/>

                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="Minimum Notice Days" Foreground="#9CA3AF" Margin="0,0,0,8"/>
                                <TextBox Text="{Binding Settings.MinimumNoticeDays}" Background="#3D3D5C" Foreground="White" Padding="12" BorderThickness="0"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Background="#4F46E5" Foreground="White" BorderThickness="0" Padding="24,12" Margin="0,0,8,0" Cursor="Hand"/>
                                <Button Content="Initialize Year" Command="{Binding InitializeYearAllocationsCommand}" Background="#059669" Foreground="White" BorderThickness="0" Padding="24,12" Margin="0,0,8,0" Cursor="Hand"/>
                                <Button Content="Process Carry Over" Command="{Binding ProcessCarryOverCommand}" Background="#0284C7" Foreground="White" BorderThickness="0" Padding="24,12" Cursor="Hand"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </TabItem>
        </TabControl>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}" Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#4F46E5"/>
                <TextBlock Text="Loading..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
