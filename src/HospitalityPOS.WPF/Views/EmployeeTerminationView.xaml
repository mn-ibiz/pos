<UserControl x:Class="HospitalityPOS.WPF.Views.EmployeeTerminationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1400" Background="#1E1E2E">
    <UserControl.Resources><BooleanToVisibilityConverter x:Key="BoolToVisConverter" /></UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <Button Content="Back" Command="{Binding GoBackCommand}" Background="#3D3D5C" Foreground="White" BorderThickness="0" Padding="16,12" Cursor="Hand"/>
            <TextBlock Grid.Column="1" Text="Employee Termination &amp; Final Settlement" FontSize="28" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button Grid.Column="2" Content="+ New Termination" Command="{Binding CreateTerminationCommand}" Background="#4F46E5" Foreground="White" BorderThickness="0" Padding="24,12" Cursor="Hand"/>
        </Grid>
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Pending Settlements" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding PendingSettlementCount}" FontSize="24" FontWeight="Bold" Foreground="#FF9800" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Total Payable" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding TotalPayable, StringFormat='KSh {0:N0}'}" FontSize="24" FontWeight="Bold" Foreground="#F44336" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="2" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Completed This Month" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding CompletedThisMonth}" FontSize="24" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="3" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="In Notice Period" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding InNoticePeriodCount}" FontSize="24" FontWeight="Bold" Foreground="#2196F3" HorizontalAlignment="Center"/></StackPanel>
            </Border>
        </Grid>
        <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedTabIndex}" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem"><Setter Property="Background" Value="#2D2D44"/><Setter Property="Foreground" Value="#9CA3AF"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TabItem"><Border Name="Border" Background="#2D2D44" CornerRadius="8,8,0,0" Margin="0,0,4,0"><ContentPresenter ContentSource="Header" Margin="20,12"/></Border><ControlTemplate.Triggers><Trigger Property="IsSelected" Value="True"><Setter TargetName="Border" Property="Background" Value="#4F46E5"/><Setter Property="Foreground" Value="White"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
            </TabControl.Resources>
            <TabItem Header="Pending Terminations">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding PendingTerminations}" SelectedItem="{Binding SelectedTermination}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Employee" Binding="{Binding EmployeeName}" Width="*"/>
                            <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="120"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding TerminationType}" Width="100"/>
                            <DataGridTextColumn Header="Last Day" Binding="{Binding LastWorkingDay, StringFormat='{}{0:dd MMM}'}" Width="100"/>
                            <DataGridTextColumn Header="Settlement" Binding="{Binding SettlementAmount, StringFormat='KSh {0:N0}'}" Width="120"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                            <DataGridTemplateColumn Header="Actions" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="View" Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#4F46E5" Foreground="White" Padding="8,4" Margin="0,0,4,0" BorderThickness="0" Cursor="Hand"/>
                                            <Button Content="Calculate" Command="{Binding DataContext.CalculateSettlementCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#0284C7" Foreground="White" Padding="8,4" Margin="0,0,4,0" BorderThickness="0" Cursor="Hand"/>
                                            <Button Content="Process" Command="{Binding DataContext.ProcessSettlementCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#059669" Foreground="White" Padding="8,4" BorderThickness="0" Cursor="Hand"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
            <TabItem Header="Settlement Details">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="16">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,16,0">
                                <TextBlock Text="Earnings" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,16"/>
                                <Border Background="#353550" CornerRadius="8" Padding="16" Margin="0,0,0,8">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="Pending Salary" Foreground="#9CA3AF"/><TextBlock Grid.Column="1" Text="{Binding SettlementDetails.PendingSalary, StringFormat='KSh {0:N0}'}" Foreground="White"/>
                                    </Grid>
                                </Border>
                                <Border Background="#353550" CornerRadius="8" Padding="16" Margin="0,0,0,8">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="Leave Encashment" Foreground="#9CA3AF"/><TextBlock Grid.Column="1" Text="{Binding SettlementDetails.LeaveEncashment, StringFormat='KSh {0:N0}'}" Foreground="White"/>
                                    </Grid>
                                </Border>
                                <Border Background="#353550" CornerRadius="8" Padding="16" Margin="0,0,0,8">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="Gratuity" Foreground="#9CA3AF"/><TextBlock Grid.Column="1" Text="{Binding SettlementDetails.Gratuity, StringFormat='KSh {0:N0}'}" Foreground="White"/>
                                    </Grid>
                                </Border>
                                <Border Background="#353550" CornerRadius="8" Padding="16" Margin="0,0,0,8">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="Bonus/Incentives" Foreground="#9CA3AF"/><TextBlock Grid.Column="1" Text="{Binding SettlementDetails.BonusIncentives, StringFormat='KSh {0:N0}'}" Foreground="White"/>
                                    </Grid>
                                </Border>
                                <Border Background="#4F46E5" CornerRadius="8" Padding="16">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="Total Earnings" Foreground="White" FontWeight="SemiBold"/><TextBlock Grid.Column="1" Text="{Binding SettlementDetails.TotalEarnings, StringFormat='KSh {0:N0}'}" Foreground="White" FontWeight="Bold"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                <TextBlock Text="Deductions" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,16"/>
                                <Border Background="#353550" CornerRadius="8" Padding="16" Margin="0,0,0,8">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="Loan Balance" Foreground="#9CA3AF"/><TextBlock Grid.Column="1" Text="{Binding SettlementDetails.LoanBalance, StringFormat='KSh {0:N0}'}" Foreground="White"/>
                                    </Grid>
                                </Border>
                                <Border Background="#353550" CornerRadius="8" Padding="16" Margin="0,0,0,8">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="Notice Period Shortfall" Foreground="#9CA3AF"/><TextBlock Grid.Column="1" Text="{Binding SettlementDetails.NoticePeriodShortfall, StringFormat='KSh {0:N0}'}" Foreground="White"/>
                                    </Grid>
                                </Border>
                                <Border Background="#353550" CornerRadius="8" Padding="16" Margin="0,0,0,8">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="Other Deductions" Foreground="#9CA3AF"/><TextBlock Grid.Column="1" Text="{Binding SettlementDetails.OtherDeductions, StringFormat='KSh {0:N0}'}" Foreground="White"/>
                                    </Grid>
                                </Border>
                                <Border Background="#F44336" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="Total Deductions" Foreground="White" FontWeight="SemiBold"/><TextBlock Grid.Column="1" Text="{Binding SettlementDetails.TotalDeductions, StringFormat='KSh {0:N0}'}" Foreground="White" FontWeight="Bold"/>
                                    </Grid>
                                </Border>
                                <Border Background="#4CAF50" CornerRadius="8" Padding="16">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="Net Settlement" Foreground="White" FontWeight="SemiBold" FontSize="16"/><TextBlock Grid.Column="1" Text="{Binding SettlementDetails.NetSettlement, StringFormat='KSh {0:N0}'}" Foreground="White" FontWeight="Bold" FontSize="16"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </Border>
            </TabItem>
            <TabItem Header="History">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding TerminationHistory}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Employee" Binding="{Binding EmployeeName}" Width="*"/>
                            <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="120"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding TerminationType}" Width="100"/>
                            <DataGridTextColumn Header="Exit Date" Binding="{Binding ExitDate, StringFormat='{}{0:dd MMM yyyy}'}" Width="120"/>
                            <DataGridTextColumn Header="Settlement" Binding="{Binding SettlementAmount, StringFormat='KSh {0:N0}'}" Width="120"/>
                            <DataGridTextColumn Header="Paid On" Binding="{Binding PaidDate, StringFormat='{}{0:dd MMM}'}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
        </TabControl>
        <Border Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}" Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"><ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#4F46E5"/><TextBlock Text="Loading..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/></StackPanel>
        </Border>
    </Grid>
</UserControl>
