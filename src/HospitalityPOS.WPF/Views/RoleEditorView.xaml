<UserControl x:Class="HospitalityPOS.WPF.Views.RoleEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:HospitalityPOS.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="900"
             Background="#1E1E2E">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibility"/>
        <converters:InverseBoolConverter x:Key="InverseBool"/>

        <Style x:Key="InputTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderBrush" Value="#4E4E6E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,14"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="52"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#6E6EAE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="20,10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#4E4E6E"/>
        </Style>

        <Style x:Key="PermissionCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="{Binding Title}"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="White"
                       HorizontalAlignment="Center"/>
        </Grid>

        <!-- Content -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel - Role Details -->
                <Border Grid.Column="0"
                        Background="#2D2D44"
                        CornerRadius="12"
                        Padding="24">
                    <StackPanel>
                        <TextBlock Text="Role Details"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   Margin="0,0,0,24"/>

                        <!-- System Role Warning -->
                        <Border Background="#3D4A5A"
                                CornerRadius="8"
                                Padding="16"
                                Margin="0,0,0,16"
                                Visibility="{Binding IsSystemRole, Converter={StaticResource BoolToVisibility}}">
                            <TextBlock Text="This is a system role. The name cannot be changed, but permissions can be modified."
                                       FontSize="12"
                                       Foreground="#A0B0C0"
                                       TextWrapping="Wrap"/>
                        </Border>

                        <!-- Role Name -->
                        <TextBlock Text="Role Name"
                                   Foreground="#A0A0B0"
                                   FontSize="14"
                                   Margin="0,0,0,8"/>
                        <TextBox Text="{Binding RoleName, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource InputTextBox}"
                                 IsEnabled="{Binding IsSystemRole, Converter={StaticResource InverseBool}}"
                                 MaxLength="50"
                                 Margin="0,0,0,16"/>

                        <!-- Role Description -->
                        <TextBlock Text="Description"
                                   Foreground="#A0A0B0"
                                   FontSize="14"
                                   Margin="0,0,0,8"/>
                        <TextBox Text="{Binding RoleDescription, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource InputTextBox}"
                                 Height="80"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalContentAlignment="Top"
                                 MaxLength="200"
                                 Margin="0,0,0,16"/>

                        <!-- Error Message -->
                        <Border Background="#3D2030"
                                CornerRadius="8"
                                Padding="16"
                                Margin="0,0,0,16"
                                Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}">
                            <TextBlock Text="{Binding ErrorMessage}"
                                       Foreground="#E74C3C"
                                       FontSize="13"
                                       TextWrapping="Wrap"/>
                        </Border>

                        <!-- Quick Actions -->
                        <TextBlock Text="Quick Actions"
                                   Foreground="#A0A0B0"
                                   FontSize="14"
                                   Margin="0,16,0,8"/>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Select All"
                                    Style="{StaticResource ActionButton}"
                                    Background="#3D5A80"
                                    MinWidth="100"
                                    Command="{Binding SelectAllPermissionsCommand}"/>
                            <Button Content="Deselect All"
                                    Style="{StaticResource ActionButton}"
                                    Background="#4E4E6E"
                                    MinWidth="100"
                                    Command="{Binding DeselectAllPermissionsCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Right Panel - Permissions -->
                <Border Grid.Column="2"
                        Background="#2D2D44"
                        CornerRadius="12"
                        Padding="24">
                    <StackPanel>
                        <TextBlock Text="Permissions"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   Margin="0,0,0,16"/>

                        <ItemsControl ItemsSource="{Binding PermissionCategories}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#1E1E2E"
                                            CornerRadius="8"
                                            Margin="0,0,0,12"
                                            Padding="16">
                                        <StackPanel>
                                            <!-- Category Header -->
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding Category}"
                                                           FontSize="15"
                                                           FontWeight="SemiBold"
                                                           Foreground="#B0B0C0"/>

                                                <Button Grid.Column="1"
                                                        Content="Toggle All"
                                                        FontSize="11"
                                                        Background="#3D3D5C"
                                                        Foreground="#A0A0B0"
                                                        Padding="8,4"
                                                        Cursor="Hand"
                                                        BorderThickness="0"
                                                        Command="{Binding DataContext.ToggleCategoryPermissionsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    CornerRadius="4"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center"
                                                                                  VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                            </Grid>

                                            <!-- Permissions List -->
                                            <ItemsControl ItemsSource="{Binding Permissions}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <CheckBox IsChecked="{Binding IsSelected}"
                                                                  Style="{StaticResource PermissionCheckBox}">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="{Binding Permission.Name}"
                                                                           Foreground="White"
                                                                           Margin="0,0,8,0"/>
                                                                <TextBlock Text="{Binding Permission.Description}"
                                                                           Foreground="#7E7E9E"
                                                                           FontSize="12"
                                                                           Visibility="{Binding Permission.Description, Converter={StaticResource StringToVisibility}}"/>
                                                            </StackPanel>
                                                        </CheckBox>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- Footer Actions -->
        <Border Grid.Row="2"
                Margin="0,24,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Cancel"
                        Style="{StaticResource ActionButton}"
                        Background="#4E4E6E"
                        HorizontalAlignment="Left"
                        Command="{Binding CancelCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}"/>

                <Button Grid.Column="1"
                        Content="Save Role"
                        Style="{StaticResource ActionButton}"
                        Background="#4CAF50"
                        MinWidth="150"
                        Command="{Binding SaveCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}"/>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3"
                Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                             Width="200"
                             Height="4"
                             Foreground="#4CAF50"/>
                <TextBlock Text="{Binding BusyMessage}"
                           Foreground="White"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
