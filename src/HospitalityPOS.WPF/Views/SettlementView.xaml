<UserControl x:Class="HospitalityPOS.WPF.Views.SettlementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:HospitalityPOS.WPF.Converters"
             xmlns:controls="clr-namespace:HospitalityPOS.WPF.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1280"
             Background="#1E1E2E">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <converters:PaymentMethodCodeToColorConverter x:Key="PaymentMethodColorConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibility"/>
        <converters:PositiveToVisibilityConverter x:Key="PositiveToVisibility"/>
        <converters:SecondsToMinSecConverter x:Key="SecondsToMinSec"/>
        <converters:MinLengthToBoolConverter x:Key="MinLengthToBool"/>

        <!-- Payment Method Button Style -->
        <Style x:Key="PaymentMethodButton" TargetType="Button">
            <Setter Property="MinHeight" Value="80"/>
            <Setter Property="MinWidth" Value="140"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Quick Amount Button Style -->
        <Style x:Key="QuickAmountButton" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#3D3D5C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#22C55E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="MinHeight" Value="50"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Outline Small Button Style for Loyalty Redemption -->
        <Style x:Key="OutlineSmallButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="BorderBrush" Value="#4E4E6E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#22C55E"/>
                                <Setter TargetName="border" Property="Background" Value="#1E1E2E"/>
                                <Setter Property="Foreground" Value="#22C55E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#22C55E"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Numeric Input TextBox Style for Loyalty Points -->
        <Style x:Key="NumericInputTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3D3D5C"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"
                                          Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#22C55E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <Border Grid.Row="0" Background="#252538">
            <Grid Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="SETTLE RECEIPT"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="#22C55E"
                               VerticalAlignment="Center"/>
                    <Rectangle Width="1" Height="24" Fill="#4E4E6E" Margin="16,0"/>
                    <TextBlock Text="{Binding Receipt.ReceiptNumber}"
                               Foreground="White"
                               FontSize="16"
                               FontWeight="Medium"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Close"
                        Background="#4E4E6E"
                        Style="{StaticResource ActionButton}"
                        MinWidth="100"
                        Margin="0,8"
                        Command="{Binding CloseAsyncCommand}"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT: Receipt Summary -->
            <Border Grid.Column="0" Background="#252538" BorderBrush="#3D3D5C" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Receipt Header -->
                    <Border Grid.Row="0" Background="#1E1E2E" Padding="16,12">
                        <StackPanel>
                            <Grid>
                                <TextBlock Text="Receipt Details"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="White"/>
                            </Grid>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Table: "
                                           Foreground="#9CA3AF"
                                           FontSize="14"/>
                                <TextBlock Text="{Binding Receipt.TableNumber}"
                                           Foreground="White"
                                           FontSize="14"
                                           FontWeight="Medium"/>
                                <TextBlock Text="  |  Customer: "
                                           Foreground="#9CA3AF"
                                           FontSize="14"
                                           Margin="16,0,0,0"/>
                                <TextBlock Text="{Binding Receipt.CustomerName, TargetNullValue='Walk-in'}"
                                           Foreground="White"
                                           FontSize="14"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Receipt Items List -->
                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  PanningMode="VerticalOnly">
                        <ItemsControl ItemsSource="{Binding ReceiptItems}" Margin="8">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#2D2D44"
                                            CornerRadius="6"
                                            Margin="4"
                                            Padding="12,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding ProductName}"
                                                           Foreground="White"
                                                           FontSize="14"
                                                           FontWeight="Medium"
                                                           TextTrimming="CharacterEllipsis"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <TextBlock Text="{Binding QuantityDisplay}"
                                                               Foreground="#9CA3AF"
                                                               FontSize="12"/>
                                                    <TextBlock Text=" x "
                                                               Foreground="#6B7280"
                                                               FontSize="12"/>
                                                    <TextBlock Text="{Binding UnitPrice, StringFormat='KSh {0:N2}'}"
                                                               Foreground="#9CA3AF"
                                                               FontSize="12"/>
                                                    <!-- Discount indicator -->
                                                    <TextBlock Text="{Binding DiscountAmount, StringFormat=' (-{0:N2})'}"
                                                               Foreground="#EF4444"
                                                               FontSize="12"
                                                               Visibility="{Binding HasDiscount, Converter={StaticResource BoolToVisibility}}"/>
                                                </StackPanel>
                                            </StackPanel>

                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding TotalAmount, StringFormat='KSh {0:N2}'}"
                                                       Foreground="#22C55E"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Receipt Totals -->
                    <Border Grid.Row="2" Background="#1E1E2E" Padding="16,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Subtotal:" Foreground="#9CA3AF" FontSize="14"/>
                                <TextBlock Text="{Binding Receipt.Subtotal, StringFormat='KSh {0:N2}'}"
                                           Foreground="White"
                                           FontSize="14"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="VAT (16%):" Foreground="#9CA3AF" FontSize="14"/>
                                <TextBlock Text="{Binding Receipt.TaxAmount, StringFormat='KSh {0:N2}'}"
                                           Foreground="White"
                                           FontSize="14"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                            <Rectangle Height="1" Fill="#3D3D5C" Margin="0,8"/>
                            <Grid>
                                <TextBlock Text="TOTAL:" Foreground="White" FontSize="20" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Receipt.TotalAmount, StringFormat='KSh {0:N2}'}"
                                           Foreground="#22C55E"
                                           FontSize="20"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Payment Summary -->
                    <Border Grid.Row="3" Background="#2D2D44" Padding="16,12">
                        <StackPanel>
                            <!-- Split Payment Header -->
                            <TextBlock Text="SPLIT PAYMENT"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       Foreground="#22C55E"
                                       Margin="0,0,0,8"
                                       Visibility="{Binding PaymentLines.Count, Converter={StaticResource BoolToVisibility}}"/>

                            <!-- Payments Made -->
                            <ItemsControl ItemsSource="{Binding PaymentLines}"
                                          Visibility="{Binding PaymentLines.Count, Converter={StaticResource BoolToVisibility}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#1E1E2E"
                                                CornerRadius="4"
                                                Margin="0,0,0,4"
                                                Padding="8,6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding MethodName}"
                                                               Foreground="White"
                                                               FontSize="13"
                                                               FontWeight="Medium"/>
                                                    <TextBlock Text="{Binding Reference, StringFormat='Ref: {0}'}"
                                                               Foreground="#6B7280"
                                                               FontSize="11"
                                                               Visibility="{Binding HasReference, Converter={StaticResource BoolToVisibility}}"/>
                                                </StackPanel>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding Amount, StringFormat='KSh {0:N2}'}"
                                                           Foreground="#22C55E"
                                                           FontSize="13"
                                                           FontWeight="SemiBold"
                                                           VerticalAlignment="Center"
                                                           Margin="8,0"/>

                                                <!-- Remove Button -->
                                                <Button Grid.Column="2"
                                                        Content="X"
                                                        Width="24"
                                                        Height="24"
                                                        FontSize="12"
                                                        FontWeight="Bold"
                                                        Background="#EF4444"
                                                        Foreground="White"
                                                        BorderThickness="0"
                                                        Cursor="Hand"
                                                        VerticalAlignment="Center"
                                                        Command="{Binding DataContext.RemovePaymentLineAsyncCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Remove this payment">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="border"
                                                                    Background="{TemplateBinding Background}"
                                                                    CornerRadius="4">
                                                                <ContentPresenter HorizontalAlignment="Center"
                                                                                  VerticalAlignment="Center"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="#DC2626"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Rectangle Height="1" Fill="#3D3D5C" Margin="0,8"
                                       Visibility="{Binding PaymentLines.Count, Converter={StaticResource BoolToVisibility}}"/>

                            <!-- Amount Paid -->
                            <Grid Margin="0,0,0,4"
                                  Visibility="{Binding PaymentLines.Count, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="Paid:" Foreground="#9CA3AF" FontSize="14"/>
                                <TextBlock Text="{Binding AmountPaid, StringFormat='KSh {0:N2}'}"
                                           Foreground="White"
                                           FontSize="14"
                                           HorizontalAlignment="Right"/>
                            </Grid>

                            <!-- Balance Due -->
                            <Grid>
                                <TextBlock Text="BALANCE DUE:" Foreground="White" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text="{Binding AmountDue, StringFormat='KSh {0:N2}'}"
                                           Foreground="#F59E0B"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- RIGHT: Payment Methods & Entry -->
            <Grid Grid.Column="1" Background="#1E1E2E">
                <!-- Payment Method Selection (visible when no method selected) -->
                <Grid Visibility="{Binding IsPaymentPanelVisible, Converter={StaticResource InverseBoolToVisibility}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Loyalty Redemption Section -->
                    <Border Grid.Row="0"
                            Background="#252538"
                            CornerRadius="8"
                            Padding="16"
                            Margin="40,24,40,0"
                            Visibility="{Binding ShowRedemptionPanel, Converter={StaticResource BoolToVisibility}}">
                        <StackPanel>
                            <!-- Header -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="Loyalty Points"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="White"/>

                                <TextBlock Grid.Column="2"
                                           FontSize="14"
                                           Foreground="#9CA3AF">
                                    <Run Text="Available: "/>
                                    <Run Text="{Binding AvailableLoyaltyPoints, StringFormat=N0}"
                                         FontWeight="SemiBold"
                                         Foreground="#22C55E"/>
                                    <Run Text=" pts"/>
                                </TextBlock>
                            </Grid>

                            <!-- Member Info Card -->
                            <Border Background="#1E1E2E"
                                    CornerRadius="6"
                                    Padding="12"
                                    Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding LoyaltyMember.Name}"
                                                   FontWeight="Medium"
                                                   FontSize="14"
                                                   Foreground="White"/>
                                        <TextBlock Text="{Binding LoyaltyMember.PhoneNumber}"
                                                   FontSize="12"
                                                   Foreground="#9CA3AF"/>
                                    </StackPanel>

                                    <!-- Tier Badge -->
                                    <Border Grid.Column="1"
                                            Background="#3B82F6"
                                            CornerRadius="4"
                                            Padding="10,4"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="{Binding LoyaltyMember.Tier}"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"
                                                   Foreground="White"/>
                                    </Border>
                                </Grid>
                            </Border>

                            <!-- Redemption Input Row -->
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="140"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Points to Redeem"
                                               FontSize="12"
                                               Foreground="#9CA3AF"
                                               Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding PointsToRedeem, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}"
                                             FontSize="18"
                                             Height="44"
                                             Padding="12,8"
                                             HorizontalContentAlignment="Right"
                                             Style="{StaticResource NumericInputTextBox}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Discount Value"
                                               FontSize="12"
                                               Foreground="#9CA3AF"
                                               Margin="0,0,0,4"/>
                                    <Border Background="#1E1E2E"
                                            CornerRadius="4"
                                            Height="44"
                                            Padding="12,8">
                                        <TextBlock VerticalAlignment="Center"
                                                   HorizontalAlignment="Right"
                                                   FontSize="18"
                                                   FontWeight="SemiBold"
                                                   Foreground="#22C55E">
                                            <Run Text="KES "/>
                                            <Run Text="{Binding PointsRedemptionValue, StringFormat=N2}"/>
                                        </TextBlock>
                                    </Border>
                                </StackPanel>
                            </Grid>

                            <!-- Constraints Info -->
                            <TextBlock FontSize="11"
                                       Foreground="#6B7280"
                                       Margin="0,0,0,8">
                                <Run Text="Min: "/>
                                <Run Text="{Binding MinimumRedemptionPoints, StringFormat=N0}"/>
                                <Run Text=" pts | Max: "/>
                                <Run Text="{Binding MaxRedeemablePoints, StringFormat=N0}"/>
                                <Run Text=" pts (50% of total)"/>
                            </TextBlock>

                            <!-- Error Message -->
                            <TextBlock Text="{Binding RedemptionError}"
                                       Foreground="#EF4444"
                                       FontSize="12"
                                       TextWrapping="Wrap"
                                       Visibility="{Binding RedemptionError, Converter={StaticResource NullToVisibility}}"
                                       Margin="0,0,0,8"/>

                            <!-- Quick Redemption Buttons -->
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <Button Content="100 pts"
                                        Command="{Binding SetRedemptionPointsCommand}"
                                        CommandParameter="100"
                                        Style="{StaticResource OutlineSmallButton}"
                                        Margin="0,0,8,0"
                                        Padding="12,6"
                                        IsEnabled="{Binding IsRedemptionEnabled}"/>
                                <Button Content="500 pts"
                                        Command="{Binding SetRedemptionPointsCommand}"
                                        CommandParameter="500"
                                        Style="{StaticResource OutlineSmallButton}"
                                        Margin="0,0,8,0"
                                        Padding="12,6"
                                        IsEnabled="{Binding IsRedemptionEnabled}"/>
                                <Button Content="Use Max"
                                        Command="{Binding RedeemMaxPointsCommand}"
                                        Style="{StaticResource OutlineSmallButton}"
                                        Margin="0,0,8,0"
                                        Padding="12,6"
                                        IsEnabled="{Binding IsRedemptionEnabled}"/>
                                <Button Content="Clear"
                                        Command="{Binding ClearRedemptionCommand}"
                                        Style="{StaticResource OutlineSmallButton}"
                                        Padding="12,6"
                                        Visibility="{Binding PointsToRedeem, Converter={StaticResource PositiveToVisibility}}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Title -->
                    <TextBlock Grid.Row="1"
                               Text="Select Payment Method"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               Margin="0,40,0,20"/>

                    <!-- Payment Method Buttons -->
                    <ItemsControl Grid.Row="2"
                                  ItemsSource="{Binding PaymentMethods}"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource PaymentMethodButton}"
                                        Background="{Binding Code, Converter={StaticResource PaymentMethodColorConverter}}"
                                        Command="{Binding DataContext.SelectPaymentMethodCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="18"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Quick Settle with Cash -->
                    <Button Grid.Row="3"
                            Content="Quick Settle (Exact Cash)"
                            Background="#22C55E"
                            Style="{StaticResource ActionButton}"
                            Width="300"
                            Margin="0,20,0,40"
                            Command="{Binding SettleWithCashAsyncCommand}"/>
                </Grid>

                <!-- Cash Payment Panel -->
                <Grid Visibility="{Binding IsPaymentPanelVisible, Converter={StaticResource BoolToVisibility}}">
                    <Border Background="#252538"
                            CornerRadius="16"
                            Margin="40"
                            MaxWidth="500"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center">
                        <Grid Margin="32">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Payment Method Title -->
                            <TextBlock Grid.Row="0"
                                       Text="{Binding SelectedPaymentMethod.Name, StringFormat='{}{0} Payment'}"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,24"/>

                            <!-- Amount Due -->
                            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,24">
                                <TextBlock Text="Amount Due"
                                           FontSize="14"
                                           Foreground="#9CA3AF"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding AmountDue, StringFormat='KSh {0:N2}'}"
                                           FontSize="32"
                                           FontWeight="Bold"
                                           Foreground="#F59E0B"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>

                            <!-- Amount Tendered Input (for cash) -->
                            <StackPanel Grid.Row="2"
                                        Visibility="{Binding IsCashPayment, Converter={StaticResource BoolToVisibility}}"
                                        Margin="0,0,0,16">
                                <TextBlock Text="Amount Tendered"
                                           FontSize="14"
                                           Foreground="#9CA3AF"
                                           Margin="0,0,0,8"/>
                                <!-- Display current amount -->
                                <Border Background="#2D2D44"
                                        CornerRadius="8"
                                        Padding="16,12"
                                        Margin="0,0,0,8">
                                    <TextBlock Text="{Binding EnteredAmountString, StringFormat='KSh {0}'}"
                                               Foreground="White"
                                               FontSize="28"
                                               FontWeight="Bold"
                                               TextAlignment="Right"/>
                                </Border>
                                <!-- Touch Numeric Keypad -->
                                <controls:NumericKeypad Value="{Binding EnteredAmountString, Mode=TwoWay}"
                                                        MaxLength="10"
                                                        Width="260"
                                                        HorizontalAlignment="Center"/>
                            </StackPanel>

                            <!-- Quick Amount Buttons (for cash) -->
                            <ItemsControl Grid.Row="3"
                                          ItemsSource="{Binding QuickAmounts}"
                                          Visibility="{Binding IsCashPayment, Converter={StaticResource BoolToVisibility}}"
                                          Margin="0,0,0,16">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel HorizontalAlignment="Center"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding StringFormat='KSh {0:N0}'}"
                                                Style="{StaticResource QuickAmountButton}"
                                                Command="{Binding DataContext.ApplyQuickAmountCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- M-Pesa Payment Panel -->
                            <StackPanel Grid.Row="3"
                                        Visibility="{Binding IsMpesaPayment, Converter={StaticResource BoolToVisibility}}"
                                        Margin="0,0,0,16">
                                <!-- M-Pesa Logo/Header -->
                                <Border Background="#4CAF50"
                                        CornerRadius="8"
                                        Padding="12,8"
                                        Margin="0,0,0,16"
                                        HorizontalAlignment="Center">
                                    <TextBlock Text="M-PESA"
                                               FontSize="20"
                                               FontWeight="Bold"
                                               Foreground="White"/>
                                </Border>

                                <TextBlock Text="Enter M-Pesa Transaction Code"
                                           FontSize="14"
                                           Foreground="#9CA3AF"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="(10 characters, e.g., QJK2XXXXXX)"
                                           FontSize="12"
                                           Foreground="#6B7280"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,12"/>

                                <!-- Transaction Code Display -->
                                <Border Background="#2D2D44"
                                        CornerRadius="8"
                                        Padding="16,12"
                                        Margin="0,0,0,8"
                                        HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding ReferenceNumber, TargetNullValue='__________'}"
                                               FontSize="28"
                                               FontWeight="Bold"
                                               Foreground="White"
                                               FontFamily="Consolas"
                                               HorizontalAlignment="Center"/>
                                </Border>

                                <!-- Validation Message -->
                                <TextBlock Text="{Binding MpesaValidationMessage}"
                                           FontSize="12"
                                           Foreground="#EF4444"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,8"
                                           Visibility="{Binding MpesaValidationMessage, Converter={StaticResource BoolToVisibility}}"/>

                                <!-- Valid Code Indicator -->
                                <TextBlock Text="Valid M-Pesa Code"
                                           FontSize="12"
                                           Foreground="#22C55E"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,8"
                                           Visibility="{Binding IsMpesaCodeValid, Converter={StaticResource BoolToVisibility}}"/>

                                <!-- Alphanumeric Keypad -->
                                <controls:AlphanumericKeypad Value="{Binding ReferenceNumber, Mode=TwoWay}"
                                                              MaxLength="10"
                                                              AutoUppercase="True"
                                                              HorizontalAlignment="Center"
                                                              Margin="0,0,0,16"/>

                                <!-- Customer Confirmation Checkbox -->
                                <Border Background="#1E1E2E"
                                        CornerRadius="8"
                                        Padding="16,12"
                                        Margin="0,8,0,0">
                                    <CheckBox IsChecked="{Binding CustomerConfirmed}"
                                              Foreground="White"
                                              FontSize="14">
                                        <CheckBox.Content>
                                            <TextBlock Text="Customer has confirmed receiving the M-Pesa message"
                                                       TextWrapping="Wrap"
                                                       MaxWidth="350"/>
                                        </CheckBox.Content>
                                    </CheckBox>
                                </Border>
                            </StackPanel>

                            <!-- Card Payment Panel -->
                            <StackPanel Grid.Row="3"
                                        Visibility="{Binding IsCardPayment, Converter={StaticResource BoolToVisibility}}"
                                        Margin="0,0,0,16">
                                <!-- Card Type Display -->
                                <Border Background="#3B82F6"
                                        CornerRadius="8"
                                        Padding="16,12"
                                        Margin="0,0,0,16"
                                        HorizontalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="CARD"
                                                   FontSize="20"
                                                   FontWeight="Bold"
                                                   Foreground="White"/>
                                        <TextBlock Text=" - "
                                                   FontSize="20"
                                                   Foreground="White"/>
                                        <TextBlock Text="{Binding SelectedPaymentMethod.Name}"
                                                   FontSize="20"
                                                   FontWeight="Bold"
                                                   Foreground="White"/>
                                    </StackPanel>
                                </Border>

                                <!-- Instructions -->
                                <TextBlock Text="Process payment on external terminal"
                                           FontSize="14"
                                           Foreground="#9CA3AF"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="then confirm here"
                                           FontSize="14"
                                           Foreground="#9CA3AF"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,16"/>

                                <!-- Last 4 Digits (Optional) -->
                                <TextBlock Text="Last 4 Digits (Optional)"
                                           FontSize="14"
                                           Foreground="#9CA3AF"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,8"/>

                                <!-- Display for last 4 digits -->
                                <Border Background="#2D2D44"
                                        CornerRadius="8"
                                        Padding="16,12"
                                        Margin="0,0,0,8"
                                        HorizontalAlignment="Center">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="****"
                                                   FontSize="28"
                                                   FontWeight="Bold"
                                                   Foreground="#6B7280"
                                                   FontFamily="Consolas"
                                                   Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding CardLastFourDigits, TargetNullValue='____'}"
                                                   FontSize="28"
                                                   FontWeight="Bold"
                                                   Foreground="White"
                                                   FontFamily="Consolas"/>
                                    </StackPanel>
                                </Border>

                                <!-- Numeric Keypad for 4 digits -->
                                <controls:NumericKeypad Value="{Binding CardLastFourDigits, Mode=TwoWay}"
                                                        MaxLength="4"
                                                        Width="260"
                                                        HorizontalAlignment="Center"
                                                        Margin="0,0,0,8"/>

                                <TextBlock Text="Skip if not required"
                                           FontSize="12"
                                           Foreground="#6B7280"
                                           FontStyle="Italic"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>

                            <!-- Other Reference Input (not M-Pesa, not Card) -->
                            <StackPanel Grid.Row="3"
                                        Margin="0,0,0,16">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding RequiresReference}" Value="True"/>
                                                    <Condition Binding="{Binding IsMpesaPayment}" Value="False"/>
                                                    <Condition Binding="{Binding IsCardPayment}" Value="False"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <TextBlock Text="Reference Number"
                                           FontSize="14"
                                           Foreground="#9CA3AF"
                                           Margin="0,0,0,8"/>
                                <TextBox Text="{Binding ReferenceNumber, UpdateSourceTrigger=PropertyChanged}"
                                         Background="#2D2D44"
                                         Foreground="White"
                                         FontSize="18"
                                         BorderBrush="#3D3D5C"
                                         BorderThickness="2"
                                         Padding="16,12"
                                         CharacterCasing="Upper"
                                         MaxLength="20"/>
                            </StackPanel>

                            <!-- Change Due (for cash) -->
                            <Border Grid.Row="4"
                                    Background="#1E1E2E"
                                    CornerRadius="8"
                                    Padding="24,16"
                                    Margin="0,0,0,24"
                                    Visibility="{Binding IsCashPayment, Converter={StaticResource BoolToVisibility}}">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="CHANGE DUE"
                                               FontSize="14"
                                               Foreground="#9CA3AF"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding ChangeDue, StringFormat='KSh {0:N2}'}"
                                               FontSize="36"
                                               FontWeight="Bold"
                                               Foreground="#22C55E"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <!-- Action Buttons -->
                            <Grid Grid.Row="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Button Grid.Column="0"
                                        Content="Cancel"
                                        Background="#4E4E6E"
                                        Style="{StaticResource ActionButton}"
                                        Margin="0,0,8,0"
                                        Command="{Binding CancelPaymentCommand}"/>

                                <Button Grid.Column="1"
                                        Content="Complete Payment"
                                        Background="#22C55E"
                                        Style="{StaticResource ActionButton}"
                                        Margin="8,0,0,0"
                                        IsEnabled="{Binding CanCompletePayment}"
                                        Command="{Binding CompletePaymentAsyncCommand}"/>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Settlement Complete Overlay -->
                <Grid Visibility="{Binding IsSettlementComplete, Converter={StaticResource BoolToVisibility}}">
                    <Border Background="#1E1E2E">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="SETTLEMENT COMPLETE"
                                       FontSize="32"
                                       FontWeight="Bold"
                                       Foreground="#22C55E"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,24"/>

                            <TextBlock Text="{Binding Receipt.ReceiptNumber}"
                                       FontSize="20"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,32"/>

                            <!-- Change Amount (if any) -->
                            <Border Background="#252538"
                                    CornerRadius="12"
                                    Padding="48,24"
                                    Margin="0,0,0,32"
                                    Visibility="{Binding FinalChangeAmount, Converter={StaticResource BoolToVisibility}}">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="CHANGE DUE"
                                               FontSize="18"
                                               Foreground="#9CA3AF"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding FinalChangeAmount, StringFormat='KSh {0:N2}'}"
                                               FontSize="48"
                                               FontWeight="Bold"
                                               Foreground="#22C55E"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <Button Content="Start New Order"
                                    Background="#22C55E"
                                    Style="{StaticResource ActionButton}"
                                    Width="250"
                                    FontSize="18"
                                    Command="{Binding StartNewOrderCommand}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Grid>

        <!-- OTP Verification Dialog Overlay -->
        <Grid Grid.RowSpan="2"
              Visibility="{Binding IsAwaitingOtpVerification, Converter={StaticResource BoolToVisibility}}"
              Background="#C0000000"
              Panel.ZIndex="100">
            <Border Background="#252538"
                    CornerRadius="12"
                    Padding="32"
                    MaxWidth="440"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                <StackPanel>
                    <!-- Header -->
                    <StackPanel HorizontalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="Verify Redemption"
                                   FontSize="24"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Foreground="White"/>
                        <TextBlock FontSize="14"
                                   HorizontalAlignment="Center"
                                   Foreground="#9CA3AF"
                                   Margin="0,8,0,0">
                            <Run Text="Code sent to "/>
                            <Run Text="{Binding OtpMaskedPhone}" FontWeight="Medium" Foreground="#22C55E"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- Redemption Summary Card -->
                    <Border Background="#1E1E2E"
                            CornerRadius="8"
                            Padding="16"
                            Margin="0,0,0,24">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Redeeming"
                                           FontSize="12"
                                           Foreground="#9CA3AF"/>
                                <TextBlock FontSize="22" FontWeight="SemiBold" Foreground="White">
                                    <Run Text="{Binding PointsToRedeem, StringFormat=N0}"/>
                                    <Run Text=" points" FontSize="14" FontWeight="Normal"/>
                                </TextBlock>
                            </StackPanel>

                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                <TextBlock Text="Discount"
                                           FontSize="12"
                                           Foreground="#9CA3AF"
                                           HorizontalAlignment="Right"/>
                                <TextBlock FontSize="22" FontWeight="SemiBold" Foreground="#22C55E">
                                    <Run Text="KES "/>
                                    <Run Text="{Binding PointsRedemptionValue, StringFormat=N2}"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- OTP Input Section -->
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Enter 6-digit verification code"
                                   FontSize="13"
                                   Foreground="#9CA3AF"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,12"/>

                        <!-- Large OTP Input -->
                        <TextBox Text="{Binding OtpInput, UpdateSourceTrigger=PropertyChanged}"
                                 FontSize="36"
                                 FontWeight="Bold"
                                 FontFamily="Consolas"
                                 TextAlignment="Center"
                                 MaxLength="6"
                                 Height="70"
                                 Padding="16"
                                 Background="#2D2D44"
                                 Foreground="White"
                                 BorderBrush="#3D3D5C"
                                 BorderThickness="2"
                                 CaretBrush="#22C55E">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding VerifyOtpAndRedeemAsyncCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                    </StackPanel>

                    <!-- Timer and Attempts Row -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Expiry Timer -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="&#xE121;"
                                       FontFamily="Segoe MDL2 Assets"
                                       Foreground="#9CA3AF"
                                       FontSize="14"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"/>
                            <TextBlock FontSize="13" Foreground="#9CA3AF">
                                <Run Text="Expires in "/>
                                <Run Text="{Binding OtpExpiresInSeconds, Converter={StaticResource SecondsToMinSec}}"
                                     FontWeight="Medium"/>
                            </TextBlock>
                        </StackPanel>

                        <!-- Attempts Remaining -->
                        <TextBlock Grid.Column="1"
                                   HorizontalAlignment="Right"
                                   FontSize="13"
                                   Visibility="{Binding OtpRemainingAttempts, Converter={StaticResource PositiveToVisibility}}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#9CA3AF"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding OtpRemainingAttempts}" Value="1">
                                            <Setter Property="Foreground" Value="#EF4444"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                            <Run Text="{Binding OtpRemainingAttempts}"/>
                            <Run Text=" attempts left"/>
                        </TextBlock>
                    </Grid>

                    <!-- Error Message -->
                    <Border Background="#3A1D1D"
                            CornerRadius="6"
                            Padding="12"
                            Margin="0,0,0,16"
                            Visibility="{Binding OtpError, Converter={StaticResource NullToVisibility}}">
                        <TextBlock Text="{Binding OtpError}"
                                   Foreground="#EF4444"
                                   FontSize="13"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"/>
                    </Border>

                    <!-- Action Buttons -->
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Content="Cancel"
                                Command="{Binding CancelOtpVerificationCommand}"
                                Background="#4E4E6E"
                                Style="{StaticResource ActionButton}"
                                Height="50"
                                FontSize="15"/>

                        <Button Grid.Column="2"
                                Command="{Binding VerifyOtpAndRedeemAsyncCommand}"
                                Background="#22C55E"
                                Style="{StaticResource ActionButton}"
                                Height="50"
                                FontSize="15">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE001;"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="16"
                                               Margin="0,0,8,0"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Text="Verify &amp; Redeem" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </Grid>

                    <!-- Resend Link -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Margin="0,20,0,0">
                        <TextBlock Text="Didn't receive the code?"
                                   FontSize="13"
                                   Foreground="#9CA3AF"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>

                        <Button Command="{Binding ResendOtpAsyncCommand}"
                                IsEnabled="{Binding CanResendOtp}"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Padding="0">
                            <TextBlock FontSize="13" Foreground="#22C55E" FontWeight="Medium">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Resend Code"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CanResendOtp}" Value="False">
                                                <Setter Property="Foreground" Value="#6B7280"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Button>

                        <TextBlock FontSize="13" Foreground="#6B7280" VerticalAlignment="Center"
                                   Visibility="{Binding CanResendOtp, Converter={StaticResource InverseBoolToVisibility}}">
                            <Run Text=" ("/>
                            <Run Text="{Binding ResendCooldownSeconds}"/>
                            <Run Text="s)"/>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="2"
                Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                             Width="200"
                             Height="4"
                             Foreground="#22C55E"/>
                <TextBlock Text="{Binding BusyMessage}"
                           Foreground="White"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
