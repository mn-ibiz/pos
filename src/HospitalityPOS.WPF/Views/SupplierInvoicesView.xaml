<UserControl x:Class="HospitalityPOS.WPF.Views.SupplierInvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:HospitalityPOS.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#1E1E2E">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        <converters:InvoiceStatusColorConverter x:Key="InvoiceStatusColorConverter" />
        <converters:InvoiceStatusTextConverter x:Key="InvoiceStatusTextConverter" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="Back"
                    Command="{Binding GoBackCommand}"
                    Background="#3D3D5C"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="16,12"
                    FontSize="14"
                    Cursor="Hand"/>

            <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Column="2"
                    Content="+ New Invoice"
                    Command="{Binding CreateInvoiceCommand}"
                    Background="#4F46E5"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="24,12"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Cursor="Hand"/>
        </Grid>

        <!-- Summary Cards -->
        <Border Grid.Row="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <StackPanel Margin="0,0,48,0">
                    <TextBlock Text="Total Outstanding"
                               FontSize="12"
                               Foreground="#8888AA"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalOutstanding, StringFormat='KSh {0:N0}'}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#FF9800"
                               HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="0,0,48,0">
                    <TextBlock Text="Overdue"
                               FontSize="12"
                               Foreground="#8888AA"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding OverdueCount}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#F44336"
                               HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Overdue Amount"
                               FontSize="12"
                               Foreground="#8888AA"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding OverdueAmount, StringFormat='KSh {0:N0}'}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#F44336"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Filter Bar -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <CheckBox Content="Show Paid Invoices"
                          IsChecked="{Binding ShowPaidInvoices}"
                          Foreground="White"
                          FontSize="14"
                          VerticalContentAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Invoice List -->
        <Border Grid.Row="3" Background="#2D2D44" CornerRadius="12">
            <Grid>
                <Border Background="#80000000"
                        CornerRadius="12"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"
                        Panel.ZIndex="10">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#4F46E5"/>
                        <TextBlock Text="Loading..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <DataGrid ItemsSource="{Binding Invoices}"
                          SelectedItem="{Binding SelectedInvoice}"
                          AutoGenerateColumns="False"
                          Background="Transparent"
                          Foreground="White"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#3D3D5C"
                          RowBackground="#2D2D44"
                          AlternatingRowBackground="#353550"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          FontSize="14"
                          RowHeight="52"
                          IsReadOnly="True">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#3D3D5C"/>
                            <Setter Property="Foreground" Value="#AAAACC"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Padding" Value="16,12"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="16,0"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#4A4A6A"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Invoice #" Binding="{Binding InvoiceNumber}" Width="120"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding InvoiceDate, StringFormat='dd/MM/yyyy'}" Width="100"/>
                        <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat='dd/MM/yyyy'}" Width="100"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat='KSh {0:N2}'}" Width="120"/>
                        <DataGridTextColumn Header="Paid" Binding="{Binding PaidAmount, StringFormat='KSh {0:N2}'}" Width="120"/>
                        <DataGridTemplateColumn Header="Outstanding" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock VerticalAlignment="Center">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="KSh {0:N2}">
                                                <Binding Path="TotalAmount"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Status" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="8,4"
                                            Background="{Binding Status, Converter={StaticResource InvoiceStatusColorConverter}}">
                                        <TextBlock Text="{Binding Status, Converter={StaticResource InvoiceStatusTextConverter}}"
                                                   Foreground="White"
                                                   FontSize="12"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <Border Grid.Row="4" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Make Payment"
                        Command="{Binding MakePaymentCommand}"
                        Background="#4CAF50"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="24,12"
                        FontSize="14"
                        Cursor="Hand"
                        Margin="0,0,12,0"/>

                <Button Content="View Payments"
                        Command="{Binding ViewPaymentsCommand}"
                        IsEnabled="{Binding SelectedInvoice, Converter={StaticResource BoolToVisConverter}}"
                        Background="#3D3D5C"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="24,12"
                        FontSize="14"
                        Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
