<UserControl x:Class="HospitalityPOS.WPF.Views.BankReconciliationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1400" Background="#1E1E2E">
    <UserControl.Resources><BooleanToVisibilityConverter x:Key="BoolToVisConverter" /></UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <Button Content="Back" Command="{Binding GoBackCommand}" Background="#3D3D5C" Foreground="White" BorderThickness="0" Padding="16,12" Cursor="Hand"/>
            <TextBlock Grid.Column="1" Text="Bank Reconciliation" FontSize="28" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="+ Bank Account" Command="{Binding CreateBankAccountCommand}" Background="#4F46E5" Foreground="White" BorderThickness="0" Padding="24,12" Margin="0,0,8,0" Cursor="Hand"/>
                <Button Content="Import Statement" Command="{Binding OpenImportDialogCommand}" Background="#059669" Foreground="White" BorderThickness="0" Padding="24,12" Cursor="Hand"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Bank Balance" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding TotalBankBalance, StringFormat='KSh {0:N0}'}" FontSize="24" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Book Balance" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding TotalBookBalance, StringFormat='KSh {0:N0}'}" FontSize="24" FontWeight="Bold" Foreground="#2196F3" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="2" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Unmatched" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding UnmatchedCount}" FontSize="24" FontWeight="Bold" Foreground="#FF9800" HorizontalAlignment="Center"/></StackPanel>
            </Border>
            <Border Grid.Column="3" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel HorizontalAlignment="Center"><TextBlock Text="Difference" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/><TextBlock Text="{Binding ReconciliationDifference, StringFormat='KSh {0:N0}'}" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/></StackPanel>
            </Border>
        </Grid>
        <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedTabIndex}" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem"><Setter Property="Background" Value="#2D2D44"/><Setter Property="Foreground" Value="#9CA3AF"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TabItem"><Border Name="Border" Background="#2D2D44" CornerRadius="8,8,0,0" Margin="0,0,4,0"><ContentPresenter ContentSource="Header" Margin="20,12"/></Border><ControlTemplate.Triggers><Trigger Property="IsSelected" Value="True"><Setter TargetName="Border" Property="Background" Value="#4F46E5"/><Setter Property="Foreground" Value="White"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
            </TabControl.Resources>
            <TabItem Header="Bank Accounts">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding BankAccounts}" SelectedItem="{Binding SelectedBankAccount}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Account Name" Binding="{Binding AccountName}" Width="*"/>
                            <DataGridTextColumn Header="Bank" Binding="{Binding BankName}" Width="150"/>
                            <DataGridTextColumn Header="Account No" Binding="{Binding AccountNumber}" Width="150"/>
                            <DataGridTextColumn Header="Balance" Binding="{Binding CurrentBalance, StringFormat='KSh {0:N0}'}" Width="120"/>
                            <DataGridTextColumn Header="Last Reconciled" Binding="{Binding LastReconciledDate, StringFormat='{}{0:dd MMM}'}" Width="120"/>
                            <DataGridTemplateColumn Header="Actions" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="View" Command="{Binding DataContext.LoadBankTransactionsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#4F46E5" Foreground="White" Padding="8,4" Margin="0,0,4,0" BorderThickness="0" Cursor="Hand"/>
                                            <Button Content="Reconcile" Command="{Binding DataContext.StartReconciliationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" Background="#059669" Foreground="White" Padding="8,4" Margin="0,0,4,0" BorderThickness="0" Cursor="Hand"/>
                                            <Button Content="Match" Command="{Binding DataContext.AutoMatchCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" Background="#0284C7" Foreground="White" Padding="8,4" BorderThickness="0" Cursor="Hand"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
            <TabItem Header="Unmatched Transactions">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding UnmatchedTransactions}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding TransactionDate, StringFormat='{}{0:dd MMM}'}" Width="100"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="Reference" Binding="{Binding Reference}" Width="120"/>
                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat='KSh {0:N0}'}" Width="120"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding TransactionType}" Width="80"/>
                            <DataGridTemplateColumn Header="Action" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Match" Command="{Binding DataContext.MatchTransactionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#4F46E5" Foreground="White" Padding="12,6" BorderThickness="0" Cursor="Hand"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
            <TabItem Header="History">
                <Border Background="#2D2D44" CornerRadius="0,8,8,8">
                    <DataGrid ItemsSource="{Binding ReconciliationHistory}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#2D2D44" AlternatingRowBackground="#353550" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Margin="16">
                        <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#3D3D5C"/><Setter Property="Foreground" Value="#AAAACC"/><Setter Property="Padding" Value="16,12"/><Setter Property="BorderThickness" Value="0"/></Style></DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Account" Binding="{Binding BankAccountName}" Width="*"/>
                            <DataGridTextColumn Header="Statement Date" Binding="{Binding StatementDate, StringFormat='{}{0:dd MMM yyyy}'}" Width="120"/>
                            <DataGridTextColumn Header="Bank Balance" Binding="{Binding BankBalance, StringFormat='KSh {0:N0}'}" Width="120"/>
                            <DataGridTextColumn Header="Book Balance" Binding="{Binding BookBalance, StringFormat='KSh {0:N0}'}" Width="120"/>
                            <DataGridTextColumn Header="Difference" Binding="{Binding Difference, StringFormat='KSh {0:N0}'}" Width="100"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                            <DataGridTextColumn Header="Completed" Binding="{Binding CompletedAt, StringFormat='{}{0:dd MMM}'}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
        </TabControl>
        <Border Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}" Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"><ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#4F46E5"/><TextBlock Text="Loading..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/></StackPanel>
        </Border>
    </Grid>
</UserControl>
