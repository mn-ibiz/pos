<UserControl x:Class="HospitalityPOS.WPF.Views.PayslipHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#1E1E2E">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="&#xE72B; Back"
                    Command="{Binding GoBackCommand}"
                    Background="#3D3D5C"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="16,12"
                    FontSize="14"
                    FontFamily="Segoe MDL2 Assets"
                    Cursor="Hand"/>

            <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding Employee.Position.Name, StringFormat='Position: {0}', FallbackValue=''}"
                           FontSize="14"
                           Foreground="#9CA3AF"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <Button Grid.Column="2"
                    Content="&#xE896; Export All"
                    Command="{Binding ExportAllPayslipsCommand}"
                    Background="#0284C7"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="24,12"
                    FontSize="14"
                    FontFamily="Segoe MDL2 Assets"
                    FontWeight="SemiBold"
                    Cursor="Hand"/>
        </Grid>

        <!-- Filter Bar -->
        <Border Grid.Row="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE787;" FontFamily="Segoe MDL2 Assets" Foreground="#9CA3AF"
                               VerticalAlignment="Center" Margin="0,0,8,0" FontSize="16"/>
                    <TextBlock Text="Year:" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding Years}"
                              SelectedItem="{Binding FilterYear}"
                              Width="100"
                              Background="#3D3D5C"
                              Foreground="White"
                              Padding="8,4"/>
                </StackPanel>

                <Button Grid.Column="2"
                        Content="&#xE72C; Refresh"
                        Command="{Binding LoadPayslipsCommand}"
                        Background="#3D3D5C"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="16,8"
                        FontSize="14"
                        FontFamily="Segoe MDL2 Assets"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Summary Cards -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,0,4,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="&#xE8D4;" FontFamily="Segoe MDL2 Assets" Foreground="#60A5FA"
                               FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="Payslips" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding PayslipCount}" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" Foreground="#4CAF50"
                               FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="Total Earnings (YTD)" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalGrossEarnings, StringFormat='KSh {0:N0}'}" FontSize="24" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" Foreground="#F44336"
                               FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="Total Deductions (YTD)" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalDeductions, StringFormat='KSh {0:N0}'}" FontSize="24" FontWeight="Bold" Foreground="#F44336" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="4,0,0,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="&#xE7BF;" FontFamily="Segoe MDL2 Assets" Foreground="#FF9800"
                               FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="Total Net Pay (YTD)" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalNetPay, StringFormat='KSh {0:N0}'}" FontSize="24" FontWeight="Bold" Foreground="#FF9800" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Payslips Grid -->
        <Border Grid.Row="3" Background="#2D2D44" CornerRadius="12">
            <Grid>
                <Border Background="#80000000"
                        CornerRadius="12"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"
                        Panel.ZIndex="10">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#4F46E5"/>
                        <TextBlock Text="Loading payslip history..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <DataGrid ItemsSource="{Binding Payslips}"
                          SelectedItem="{Binding SelectedPayslip}"
                          AutoGenerateColumns="False"
                          Background="Transparent"
                          Foreground="White"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#3D3D5C"
                          RowBackground="#2D2D44"
                          AlternatingRowBackground="#353550"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          FontSize="14"
                          RowHeight="48"
                          IsReadOnly="True">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#3D3D5C"/>
                            <Setter Property="Foreground" Value="#AAAACC"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Padding" Value="16,12"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="16,0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#4A4A6A"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Period" Binding="{Binding PayrollPeriod.PeriodName}" Width="150"/>
                        <DataGridTextColumn Header="Pay Date" Binding="{Binding PayrollPeriod.PayDate, StringFormat='d'}" Width="100"/>
                        <DataGridTextColumn Header="Basic Salary" Binding="{Binding BasicSalary, StringFormat='KSh {0:N0}'}" Width="120"/>
                        <DataGridTextColumn Header="Allowances" Binding="{Binding Allowances, StringFormat='KSh {0:N0}'}" Width="100"/>
                        <DataGridTextColumn Header="Overtime" Binding="{Binding OvertimePay, StringFormat='KSh {0:N0}'}" Width="100"/>
                        <DataGridTextColumn Header="Gross" Binding="{Binding GrossEarnings, StringFormat='KSh {0:N0}'}" Width="110">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Deductions" Binding="{Binding TotalDeductions, StringFormat='KSh {0:N0}'}" Width="110">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#F44336"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Net Pay" Binding="{Binding NetPay, StringFormat='KSh {0:N0}'}" Width="110">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#FF9800"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="Status" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="8,4" HorizontalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#FF9800"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPaid}" Value="True">
                                                        <Setter Property="Background" Value="#4CAF50"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Foreground="White" FontSize="11" HorizontalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="Pending"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPaid}" Value="True">
                                                            <Setter Property="Text" Value="Paid"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <Border Grid.Row="4" Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="&#xE890; View Payslip"
                        Command="{Binding ViewPayslipCommand}"
                        Background="#4F46E5"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="24,12"
                        FontSize="14"
                        FontFamily="Segoe MDL2 Assets"
                        Cursor="Hand"
                        Margin="0,0,8,0"/>

                <Button Content="&#xE749; Print Payslip"
                        Command="{Binding PrintPayslipCommand}"
                        Background="#059669"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="24,12"
                        FontSize="14"
                        FontFamily="Segoe MDL2 Assets"
                        Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
