<UserControl x:Class="HospitalityPOS.WPF.Views.Marketing.SmsTemplateListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1000"
             Background="#1E1E2E">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="12,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#3D3D5C"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#16162B" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="&#xE72B;" FontFamily="Segoe MDL2 Assets"
                        Command="{Binding GoBackCommand}"
                        Background="Transparent" BorderThickness="0"
                        Foreground="#9CA3AF" FontSize="16" Padding="8"
                        Cursor="Hand" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Margin="16,0">
                    <TextBlock Text="SMS Templates" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Create and manage reusable message templates"
                               Foreground="#9CA3AF" FontSize="13" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Category Filter -->
                <ComboBox Grid.Column="2" Width="180" Margin="0,0,16,0"
                          ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding SelectedCategory}"
                          DisplayMemberPath="DisplayName"
                          Background="#2D2D44" Foreground="White"
                          BorderBrush="#3D3D5C" Padding="12,8"/>

                <Button Grid.Column="3" Style="{StaticResource ModernButton}"
                        Background="#10B981" Content="New Template"
                        Command="{Binding CreateTemplateCommand}"/>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Template List -->
            <Border Grid.Column="0" Background="#252538" CornerRadius="12" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" FontSize="16" FontWeight="SemiBold" Foreground="White"
                               Margin="0,0,0,16">
                        <Run Text="Templates ("/>
                        <Run Text="{Binding Templates.Count}"/>
                        <Run Text=")"/>
                    </TextBlock>

                    <ListBox Grid.Row="1" ItemsSource="{Binding Templates}"
                             SelectedItem="{Binding SelectedTemplate}"
                             Background="Transparent" BorderThickness="0">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="0,0,0,8"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd" Background="#2D2D44" CornerRadius="8" Padding="12">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#3D3D5C"/>
                                                    <Setter TargetName="Bd" Property="BorderBrush" Value="#F59E0B"/>
                                                    <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#353550"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="White"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <Border Padding="4,2" CornerRadius="3" Margin="0,0,8,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#374151"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Category}" Value="Promotion">
                                                                <Setter Property="Background" Value="#422006"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Category}" Value="Loyalty">
                                                                <Setter Property="Background" Value="#052E16"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Category}" Value="Transactional">
                                                                <Setter Property="Background" Value="#1E3A5F"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Category}" FontSize="10">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#9CA3AF"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Category}" Value="Promotion">
                                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Category}" Value="Loyalty">
                                                                    <Setter Property="Foreground" Value="#10B981"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Category}" Value="Transactional">
                                                                    <Setter Property="Foreground" Value="#3B82F6"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                            <TextBlock Foreground="#6B7280" FontSize="11">
                                                <Run Text="{Binding CharacterCount}"/>
                                                <Run Text="chars |"/>
                                                <Run Text="{Binding SmsSegments}"/>
                                                <Run Text="SMS"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                        <Button Content="&#xE70F;" FontFamily="Segoe MDL2 Assets"
                                                Command="{Binding DataContext.EditTemplateCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"
                                                Background="Transparent" Foreground="#9CA3AF"
                                                BorderThickness="0" Padding="6" Cursor="Hand"
                                                ToolTip="Edit"/>
                                        <Button Content="&#xE8C8;" FontFamily="Segoe MDL2 Assets"
                                                Command="{Binding DataContext.DuplicateTemplateCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"
                                                Background="Transparent" Foreground="#9CA3AF"
                                                BorderThickness="0" Padding="6" Cursor="Hand"
                                                ToolTip="Duplicate"/>
                                        <Button Content="&#xE74D;" FontFamily="Segoe MDL2 Assets"
                                                Command="{Binding DataContext.DeleteTemplateCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"
                                                Background="Transparent" Foreground="#EF4444"
                                                BorderThickness="0" Padding="6" Cursor="Hand"
                                                ToolTip="Delete"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Preview Panel -->
            <Border Grid.Column="2" Background="#252538" CornerRadius="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="Preview" FontSize="16" FontWeight="SemiBold" Foreground="White"
                               Margin="0,0,0,16"/>

                    <Border Background="#2D2D44" CornerRadius="8" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Sample Message" Foreground="#9CA3AF" FontSize="11" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding PreviewMessage}" Foreground="White" TextWrapping="Wrap"
                                       FontSize="14" LineHeight="22"/>
                        </StackPanel>
                    </Border>

                    <!-- Placeholder Guide -->
                    <TextBlock Text="Available Placeholders" FontSize="14" FontWeight="SemiBold"
                               Foreground="White" Margin="0,24,0,12"/>
                    <Border Background="#2D2D44" CornerRadius="8" Padding="12">
                        <StackPanel>
                            <TextBlock Text="{}{CustomerName} - Full name" Foreground="#9CA3AF" FontSize="12"/>
                            <TextBlock Text="{}{FirstName} - First name only" Foreground="#9CA3AF" FontSize="12" Margin="0,4,0,0"/>
                            <TextBlock Text="{}{Points} - Current points balance" Foreground="#9CA3AF" FontSize="12" Margin="0,4,0,0"/>
                            <TextBlock Text="{}{TierName} - Loyalty tier" Foreground="#9CA3AF" FontSize="12" Margin="0,4,0,0"/>
                            <TextBlock Text="{}{StoreName} - Store name" Foreground="#9CA3AF" FontSize="12" Margin="0,4,0,0"/>
                            <TextBlock Text="{}{ExpiryDate} - Expiry date" Foreground="#9CA3AF" FontSize="12" Margin="0,4,0,0"/>
                            <TextBlock Text="{}{DiscountPercent} - Discount value" Foreground="#9CA3AF" FontSize="12" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="2" Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="#F59E0B"/>
                <TextBlock Text="{Binding BusyMessage}" Foreground="White" FontSize="14"
                           HorizontalAlignment="Center" Margin="0,16,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
