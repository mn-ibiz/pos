<UserControl x:Class="HospitalityPOS.WPF.Views.Marketing.CustomerSegmentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1000"
             Background="#1E1E2E">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="12,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#3D3D5C"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#16162B" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="&#xE72B;" FontFamily="Segoe MDL2 Assets"
                        Command="{Binding GoBackCommand}"
                        Background="Transparent" BorderThickness="0"
                        Foreground="#9CA3AF" FontSize="16" Padding="8"
                        Cursor="Hand" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Margin="16,0">
                    <TextBlock Text="Customer Segments" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Define customer groups for targeted campaigns"
                               Foreground="#9CA3AF" FontSize="13" Margin="0,4,0,0"/>
                </StackPanel>

                <Button Grid.Column="2" Style="{StaticResource ModernButton}"
                        Background="#10B981" Content="New Segment"
                        Command="{Binding CreateSegmentCommand}"/>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Segment List -->
            <Border Grid.Column="0" Background="#252538" CornerRadius="12" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" FontSize="16" FontWeight="SemiBold" Foreground="White"
                               Margin="0,0,0,16">
                        <Run Text="Segments ("/>
                        <Run Text="{Binding Segments.Count}"/>
                        <Run Text=")"/>
                    </TextBlock>

                    <ListBox Grid.Row="1" ItemsSource="{Binding Segments}"
                             SelectedItem="{Binding SelectedSegment}"
                             Background="Transparent" BorderThickness="0">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="0,0,0,8"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd" Background="#2D2D44" CornerRadius="8" Padding="12">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#3D3D5C"/>
                                                    <Setter TargetName="Bd" Property="BorderBrush" Value="#3B82F6"/>
                                                    <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#353550"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="White"/>
                                        <TextBlock Text="{Binding Description}" Foreground="#6B7280" FontSize="12"
                                                   TextTrimming="CharacterEllipsis" MaxWidth="300" Margin="0,2,0,0"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                            <Border Background="#1E3A5F" Padding="6,2" CornerRadius="3" Margin="0,0,8,0">
                                                <TextBlock Foreground="#3B82F6" FontSize="12" FontWeight="SemiBold">
                                                    <Run Text="{Binding CachedCount}"/>
                                                    <Run Text="customers"/>
                                                </TextBlock>
                                            </Border>
                                            <Border Background="#374151" Padding="4,2" CornerRadius="3">
                                                <TextBlock Text="{Binding SegmentType}" Foreground="#9CA3AF" FontSize="10"/>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="&#xE72C;" FontFamily="Segoe MDL2 Assets"
                                                Command="{Binding DataContext.RefreshCountCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"
                                                Background="Transparent" Foreground="#9CA3AF"
                                                BorderThickness="0" Padding="6" Cursor="Hand"
                                                ToolTip="Refresh Count"/>
                                        <Button Content="&#xE8BD;" FontFamily="Segoe MDL2 Assets"
                                                Command="{Binding DataContext.CreateCampaignFromSegmentCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"
                                                Background="Transparent" Foreground="#10B981"
                                                BorderThickness="0" Padding="6" Cursor="Hand"
                                                ToolTip="Create Campaign"/>
                                        <Button Content="&#xE70F;" FontFamily="Segoe MDL2 Assets"
                                                Command="{Binding DataContext.EditSegmentCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"
                                                Background="Transparent" Foreground="#9CA3AF"
                                                BorderThickness="0" Padding="6" Cursor="Hand"
                                                ToolTip="Edit"/>
                                        <Button Content="&#xE74D;" FontFamily="Segoe MDL2 Assets"
                                                Command="{Binding DataContext.DeleteSegmentCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"
                                                Background="Transparent" Foreground="#EF4444"
                                                BorderThickness="0" Padding="6" Cursor="Hand"
                                                ToolTip="Delete"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Preview Panel -->
            <Border Grid.Column="2" Background="#252538" CornerRadius="12" Padding="16">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Segment Preview" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                        <ProgressBar Grid.Column="1" IsIndeterminate="True" Width="20" Height="20"
                                     Visibility="{Binding IsPreviewLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </Grid>

                    <!-- Customer Count -->
                    <Border Background="#2D2D44" CornerRadius="8" Padding="16" Margin="0,16,0,0">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="{Binding PreviewCount}" FontSize="36" FontWeight="Bold"
                                       Foreground="#3B82F6" HorizontalAlignment="Center"/>
                            <TextBlock Text="Matching Customers" Foreground="#9CA3AF" FontSize="13"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Sample Customers -->
                    <TextBlock Text="Sample Customers" FontSize="14" FontWeight="SemiBold"
                               Foreground="White" Margin="0,24,0,12"/>

                    <ItemsControl ItemsSource="{Binding PreviewCustomers}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#2D2D44" CornerRadius="6" Padding="12" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding PhoneNumber}" Foreground="#6B7280" FontSize="12"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                            <TextBlock Foreground="#F59E0B" FontSize="12" HorizontalAlignment="Right">
                                                <Run Text="{Binding PointsBalance, StringFormat=N0}"/>
                                                <Run Text="pts"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding TierName}" Foreground="#6B7280" FontSize="11"
                                                       HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Empty State -->
                    <TextBlock Text="Select a segment to preview customers"
                               Foreground="#6B7280" FontStyle="Italic" FontSize="13"
                               HorizontalAlignment="Center" Margin="0,20,0,0"
                               Visibility="{Binding SelectedSegment, Converter={StaticResource NullToVisibleConverter}, FallbackValue=Visible}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="2" Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="#F59E0B"/>
                <TextBlock Text="{Binding BusyMessage}" Foreground="White" FontSize="14"
                           HorizontalAlignment="Center" Margin="0,16,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
